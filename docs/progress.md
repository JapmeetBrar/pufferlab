# PufferLab Progress Ledger

- **Active goal:** deliver [Milestone 4](milestone-4-execution.md) through the reviewed engineering
  loop: an operator-ready live workflow, exact owned cleanup, provider-free evaluation gates, and
  repeatable browser/accessibility CI.
- **Goal status:** implementing
- **Last updated:** 2026-08-23
- **Orchestrator:** root agent
- **Dedicated reviewer:** reviewer agent

This file is the versioned execution snapshot. Update it on an active task branch at each handoff,
review decision, or blocker. Between ledger updates, GitHub PR/check/merge history is canonical for
completed merges. The orchestrator batches that evidence into the next planned update and uses one
finalization PR at goal completion; the finalization PR's own merge stays canonical in GitHub.
Process details live in [`engineering-loop.md`](engineering-loop.md).

- **Main enforcement:** active GitHub ruleset
  [`21190317`](https://github.com/JapmeetBrar/pufferlab/rules/21190317) requires pull requests plus
  successful `Backend` and `Frontend` checks, blocks deletion/non-fast-forward updates, and has no
  bypass actors.
- **Identity limitation:** all agents share one GitHub identity, so the ruleset requires zero GitHub
  approvals, and extra approval for unattributed changes is disabled. Independent agent review and
  reviewer-only merge remain required and are recorded here or in the canonical PR history.

## Work queue

| ID | Deliverable | Owner | Branch / PR | Status | Evidence / next action |
|---|---|---|---|---|---|
| F0 | Product brief and implementation plan | foundation_fixes | `codex/pufferlab-plan` / PR #1 | verified | Reviewer approved after fixes and merged as `561a554`; plan is now on `main`. |
| F1 | Contract-first backend/frontend scaffold | foundation_fixes | `codex/t0-contract-scaffold` / PR #2 | verified | Reviewer reran contract gates, approved, and merged as `6acaf96`; GitHub Backend/Frontend CI passed. |
| LOOP | Agent policy, review runbook, and progress ledger | foundation_fixes | `codex/engineering-loop` / [PR #3](https://github.com/JapmeetBrar/pufferlab/pull/3) | verified | Independent reviewer approved the repaired workflow; PR #3 merged to protected `main` as `78d5ddf` with Backend and Frontend checks green. |
| M1-A | [Narrow turbopuffer provider with fake and opt-in live tests](implementation-plan.md#task-t2) | provider_worker | `codex/t2-turbopuffer-provider` / [PR #5](https://github.com/JapmeetBrar/pufferlab/pull/5) | verified | Dedicated review verified detached SDK errors, frozen `k3` schema support, exact fake/live write coverage, and the corrected ledger. PR #5 merged to protected `main` as `23ccd79`; post-merge Backend and Frontend checks passed. Multi-query, server RRF, recall/warm hints, and live hybrid parity remain deferred to M1-C/T5. |
| M1-B | [Deterministic tiny fixture and idempotent ingestion command](implementation-plan.md#task-t3) | fixture_worker | `codex/t3-tiny-ingestion` / [PR #4](https://github.com/JapmeetBrar/pufferlab/pull/4) | verified | Dedicated review approved the exact remote-ID/count readiness repair, real-SDK metadata normalization, observed ANN metric verification, and FTS byte-limit fix. PR #4 squash-merged to protected `main` as `7086d2b`; post-merge Backend and Frontend checks passed. Concrete sentence-transformers execution and the user-facing ingestion command remain later integration work. |
| M1-C | [Retrieval orchestration](implementation-plan.md#task-t5) and [compare API](implementation-plan.md#task-t8) | provider_worker | `codex/t5-compare-api` / [PR #6](https://github.com/JapmeetBrar/pufferlab/pull/6) | verified | Dedicated review approved the schema-bound filter validation and production ordering repair. PR #6 merged to protected `main` as `5ca296c`; post-merge Backend and Frontend checks passed. Hybrid multi-query/server RRF, warm hints, and eval execution remain deferred beyond the Milestone 1 comparison path. |
| M1-D | [Playground query UI with side-by-side results](implementation-plan.md#task-t8) | frontend_worker | `codex/t8-playground` / [PR #8](https://github.com/JapmeetBrar/pufferlab/pull/8) | verified | Dedicated review approved the generated-type-only Playground, exact safe request, direct-error handling, stable URL state, responsive/accessibility behavior, and source/build exposure scans. PR #8 squash-merged to protected `main` as `b7ca478`; post-merge Backend and Frontend checks passed. |
| M1-E | [Tiny-fixture ingestion command](implementation-plan.md#t7--cli-workflows) and [setup documentation](implementation-plan.md#task-t11) | fixture_worker | `codex/m1-ingest-cli` / [PR #7](https://github.com/JapmeetBrar/pufferlab/pull/7) | verified | Dedicated review approved the installed manifest-pinned `pufferlab dataset ingest-tiny` command, owned namespace safety, shared normalized BGE runtime, idempotent exact-readiness coverage, redacted failures, and fresh-clone setup guidance. PR #7 squash-merged to protected `main` as `a5442f9`; post-merge Backend and Frontend checks passed. A real account credential has not been consumed, so live vertical-slice verification remains pending. |
| M1-F | [Live vertical-slice verification](live-verification.md) | root + reviewer | `codex/m1-live-verification` / [PR #9](https://github.com/JapmeetBrar/pufferlab/pull/9) | verified | [Dedicated final review](https://github.com/JapmeetBrar/pufferlab/pull/9#issuecomment-5382791048) approved the exact evidence head; PR #9 squash-merged as `c1fa093`. The [canonical post-merge record](https://github.com/JapmeetBrar/pufferlab/pull/9#issuecomment-5382795239) confirms protected `main` Backend and Frontend checks passed. |
| M2-0 | [Milestone 2 coordination and contract audit](milestone-2-execution.md#m2-0--coordination-and-contract-audit) | root | `codex/m2-plan` / [PR #10](https://github.com/JapmeetBrar/pufferlab/pull/10) | verified | [Dedicated re-review](https://github.com/JapmeetBrar/pufferlab/pull/10#issuecomment-5383856977) approved the repaired source, licensing, attribution, and tracked-artifact boundary. PR #10 squash-merged as `42c3da5a912c565898ecba2c5e4504d4395994db`; the [canonical protected-main record](https://github.com/JapmeetBrar/pufferlab/pull/10#issuecomment-5383860571) confirms Backend and Frontend checks passed. |
| M2-A | [Pure evaluation engine](milestone-2-execution.md#m2-a--pure-evaluation-engine) | eval_worker | `codex/m2-eval-engine` / [PR #11](https://github.com/JapmeetBrar/pufferlab/pull/11) | verified | [Dedicated re-review](https://github.com/JapmeetBrar/pufferlab/pull/11#issuecomment-5383928856) approved the bounded ledger repair with evaluation code unchanged. PR #11 squash-merged as `7062d6b5fc27d6d7ae525967e3dbf7f60080966c`; the [canonical record](https://github.com/JapmeetBrar/pufferlab/pull/11#issuecomment-5383931950) confirms protected-main Backend and Frontend checks passed. |
| M2-B | [SQLite persistence and run lifecycle](milestone-2-execution.md#m2-b--sqlite-persistence-and-run-lifecycle) | persistence worker | `codex/m2-persistence-jobs` / [PR #12](https://github.com/JapmeetBrar/pufferlab/pull/12) | verified | [Independent re-review](https://github.com/JapmeetBrar/pufferlab/pull/12#issuecomment-5383977502) accepted repair `ef6a998`; PR #12 squash-merged as `55a72410b241b9baa46899448a829de2b6b9e130`. The [canonical record](https://github.com/JapmeetBrar/pufferlab/pull/12#issuecomment-5383980690) confirms protected-main Backend and Frontend checks passed. |
| M2-C | [Hybrid retrieval and local reranking](milestone-2-execution.md#m2-c--hybrid-retrieval-and-local-reranking) | root retrieval worker | `codex/m2-hybrid-retrieval` / [PR #13](https://github.com/JapmeetBrar/pufferlab/pull/13) | verified | [Independent re-review](https://github.com/JapmeetBrar/pufferlab/pull/13#issuecomment-5384004506) approved exact head `b79bb5c`; PR #13 reviewer-only squash-merged as `48f4e29`. The [canonical merge record](https://github.com/JapmeetBrar/pufferlab/pull/13#issuecomment-5384007820) confirms protected-main Backend and Frontend checks passed. |
| M2-D | [CQADupStack Unix dataset foundation](milestone-2-execution.md#m2-d--cqadupstack-unix-dataset-foundation) | dataset_worker | `codex/m2-unix-dataset` / [PR #14](https://github.com/JapmeetBrar/pufferlab/pull/14) | verified | [Independent re-review](https://github.com/JapmeetBrar/pufferlab/pull/14#issuecomment-5384120560) approved repaired head `7187702`; PR #14 reviewer-only squash-merged as `31387b6b62cc938a3b0d107c4c4ac5e0fd7fb4c9`. The [canonical record](https://github.com/JapmeetBrar/pufferlab/pull/14#issuecomment-5384124747) confirms protected-main Backend and Frontend checks passed, including full-history audit `160/357/19`. |
| M2-E | [Evaluation service and CLI](milestone-2-execution.md#m2-e--evaluation-application-service-and-cli) | root integration worker + delegated seam workers | `codex/m2-eval-cli` / [PR #15](https://github.com/JapmeetBrar/pufferlab/pull/15) | verified | [Independent re-review](https://github.com/JapmeetBrar/pufferlab/pull/15#issuecomment-5384253856) reproduced both repair paths, approved the exact head, and reviewer-only squash-merged PR #15 as `0e91e2bd24194e375e0dd5d6b0e12b729a3409d0`. The [canonical record](https://github.com/JapmeetBrar/pufferlab/pull/15#issuecomment-5384257932) confirms protected-main Backend and Frontend passed. |
| M2-F | [Live execution and goal finalization](milestone-2-execution.md#m2-f--live-execution-and-goal-finalization) | root + live_harness worker + reviewer | `codex/m2-live-finalization` / [PR #16](https://github.com/JapmeetBrar/pufferlab/pull/16) | verified | [Dedicated final review](https://github.com/JapmeetBrar/pufferlab/pull/16#issuecomment-5384603898) approved the exact evidence head; PR #16 reviewer-only squash-merged as `a1f1197bbd600800f513b7079edb57629c3ba955`. The [canonical record](https://github.com/JapmeetBrar/pufferlab/pull/16#issuecomment-5384608895) confirms protected-main Backend and Frontend checks passed. |
| M3-0 | [Milestone 3 architecture and contract audit](milestone-3-execution.md#m3-0--architecture-and-contract-audit) | root | `codex/m3-plan` / [PR #17](https://github.com/JapmeetBrar/pufferlab/pull/17) | verified | [Final independent re-review](https://github.com/JapmeetBrar/pufferlab/pull/17#issuecomment-5384987276) approved the exact repaired plan. PR #17 reviewer-only squash-merged as `22abcc8b47fe03d200b82f299c4502c77ea1babf`; the [canonical record](https://github.com/JapmeetBrar/pufferlab/pull/17#issuecomment-5384993596) confirms protected-main Backend and Frontend passed. |
| M3-A | [Contract freeze](milestone-3-execution.md#m3-a--contract-freeze) | eval_application | `codex/m3-contracts` / [PR #18](https://github.com/JapmeetBrar/pufferlab/pull/18) | verified | [Final independent re-review](https://github.com/JapmeetBrar/pufferlab/pull/18#issuecomment-5385231544) rejected every prior timing/provenance exploit and approved the exact contract head. PR #18 reviewer-only squash-merged as `5a66216f58d71a58787e90a4494fa771b98e0513`; the [canonical record](https://github.com/JapmeetBrar/pufferlab/pull/18#issuecomment-5385236264) confirms protected-main Backend and Frontend passed. |
| M3-B | [Durable read models and eval HTTP surface](milestone-3-execution.md#m3-b--durable-read-models-and-eval-http-surface) | eval_application + fixture_worker | `codex/m3-eval-read-api` / [PR #19](https://github.com/JapmeetBrar/pufferlab/pull/19) | verified | [Independent re-review](https://github.com/JapmeetBrar/pufferlab/pull/19#issuecomment-5385428827) closed the lifecycle and exception-detachment blockers. PR #19 reviewer-only squash-merged as `87814c1caab34e263859ce7302ed14fb2627cd22`; the [canonical record](https://github.com/JapmeetBrar/pufferlab/pull/19#issuecomment-5385435196) confirms protected-main Backend and Frontend passed. |
| M3-C | [Run dashboard](milestone-3-execution.md#m3-c--run-dashboard) | frontend_worker | `codex/m3-run-dashboard` / [PR #20](https://github.com/JapmeetBrar/pufferlab/pull/20) | verified | [Independent review](https://github.com/JapmeetBrar/pufferlab/pull/20#issuecomment-5385563568) approved the exact dashboard head after local gates and root browser QA. PR #20 reviewer-only squash-merged as `ed549df87307e877cbe5da8bb1a463af4ebabc87`; the [canonical record](https://github.com/JapmeetBrar/pufferlab/pull/20#issuecomment-5385569932) confirms protected-main Backend and Frontend passed. |
| M3-D | [Dataset-bound execution and control runtime](milestone-3-execution.md#m3-d--dataset-bound-execution-and-control-runtime) | root + live_harness + eval_application | `codex/m3-eval-control-runtime` / [PR #21](https://github.com/JapmeetBrar/pufferlab/pull/21) | verified | [Independent re-review](https://github.com/JapmeetBrar/pufferlab/pull/21#issuecomment-5385708016) reproduced all eleven repaired exception-detachment paths and approved the exact head. PR #21 reviewer-only squash-merged as `ef55645bd4edcc2fc739f0219bfb23b4d51a8d3c`; the [canonical record](https://github.com/JapmeetBrar/pufferlab/pull/21#issuecomment-5385713819) confirms protected-main Backend and Frontend passed. |
| M3-E | [Regression deep links and observable query forensics](milestone-3-execution.md#m3-e--regression-deep-links-and-observable-query-forensics) | live_harness + frontend_worker | `codex/m3-query-forensics` / [PR #22](https://github.com/JapmeetBrar/pufferlab/pull/22) | verified | [Independent re-review](https://github.com/JapmeetBrar/pufferlab/pull/22#issuecomment-5386008445) reproduced the repaired source-authentication, repeated-cancellation, and detached-close paths and approved exact head `1901f3f`. PR #22 reviewer-only squash-merged as `43ee692`; the [canonical record](https://github.com/JapmeetBrar/pufferlab/pull/22#issuecomment-5386015555) confirms protected-main Backend and Frontend passed. |
| M3-F | [Interview QA and goal finalization](milestone-3-execution.md#m3-f--interview-qa-and-finalization) | root + reviewer | `codex/m3-finalization` / [PR #23](https://github.com/JapmeetBrar/pufferlab/pull/23) | verified | [Independent re-review](https://github.com/JapmeetBrar/pufferlab/pull/23#issuecomment-5386152705) closed the historical-anchor blocker and reviewer-only squash-merged PR #23 as `a7c2800`. The [canonical record](https://github.com/JapmeetBrar/pufferlab/pull/23#issuecomment-5386159858) confirms protected-main Backend and Frontend checks passed. |
| M4-0 | [Milestone 4 architecture and gap audit](milestone-4-execution.md#m4-0--architecture-and-gap-audit) | root | `codex/m4-plan` / [PR #24](https://github.com/JapmeetBrar/pufferlab/pull/24) | verified | [Independent exact-head re-review](https://github.com/JapmeetBrar/pufferlab/pull/24#issuecomment-5387168021) approved the one-shot metadata and corrupted-receipt repairs. PR #24 reviewer-only squash-merged as `0e244ce`; the [canonical record](https://github.com/JapmeetBrar/pufferlab/pull/24#issuecomment-5387174910) confirms protected-main Backend and Frontend passed. |
| M4-A | [Contract freeze](milestone-4-execution.md#m4-a--contract-freeze) | contract_worker | `codex/m4-contracts` / [PR #25](https://github.com/JapmeetBrar/pufferlab/pull/25) | verified | [Independent exact-head re-review](https://github.com/JapmeetBrar/pufferlab/pull/25#issuecomment-5387261362) verified both approximate-rate attacks reject and every exact failed-count rate round-trips. PR #25 reviewer-only squash-merged as `8abdb32`; the [canonical record](https://github.com/JapmeetBrar/pufferlab/pull/25#issuecomment-5387268448) confirms protected-main Backend and Frontend passed. |
| M4-B | [Readiness, doctor, and one-worker serve](milestone-4-execution.md#m4-b--readiness-doctor-and-one-worker-serve) | provider_probe + readiness_core + doctor_worker + serve_worker | `codex/m4-readiness` / [PR #26](https://github.com/JapmeetBrar/pufferlab/pull/26) | verified | [Independent final review](https://github.com/JapmeetBrar/pufferlab/pull/26#issuecomment-5388026484) approved exact head `586a190`; PR #26 reviewer-only squash-merged as `82e2f59`. The [canonical record](https://github.com/JapmeetBrar/pufferlab/pull/26#issuecomment-5388034421) confirms fresh protected-main Backend and Frontend checks passed. |
| M4-C | [Owned tiny lifecycle](milestone-4-execution.md#m4-c--owned-tiny-lifecycle) | namespace lifecycle worker | `codex/m4-owned-tiny` / [PR #29](https://github.com/JapmeetBrar/pufferlab/pull/29) | implementing | Independent re-review found a remaining terminal-removal race after the named replacement is verified but before its fixed path is unlinked. The focused repair must make that final unlink identity-conditional, preserve every substitute and authenticated recovery receipt, reproduce the exact post-verification swap deterministically, then integrate verified M4-F `main` and rerun combined focused/browser/full gates before returning to `review_requested`. |
| M4-D | [Pure gate engine](milestone-4-execution.md#m4-d--pure-gate-engine) | gate_core | `codex/m4-gate-core` / [PR #27](https://github.com/JapmeetBrar/pufferlab/pull/27) | verified | [Independent exact-head review](https://github.com/JapmeetBrar/pufferlab/pull/27#issuecomment-5388111130) approved `dbe6ecf`; PR #27 reviewer-only squash-merged as `ac78452`. The [canonical record](https://github.com/JapmeetBrar/pufferlab/pull/27#issuecomment-5388121607) confirms fresh protected-main Backend and Frontend checks passed. |

## Review history

| Date | Task / PR | Transition | Evidence / next action |
|---|---|---|---|
| 2026-08-23 | M4-C / [PR #29](https://github.com/JapmeetBrar/pufferlab/pull/29) | `review_requested → changes_requested → implementing` (second re-review) | Independent re-review identified an analogous terminal-remove window in `_remove_receipt_cas`: after the exchanged-in replacement is verified at the fixed name, a same-account substitute can arrive before `os.unlink(receipt.json)` and be deleted. The repair must bind the destructive unlink to the exact replacement identity (or restore/fail closed), preserve the substitute and authenticated prior receipt on every ambiguity, add an exact deterministic attack, audit all analogous fixed-path cleanup, normally merge verified M4-F `main`, and rerun the combined gates before another immutable handoff. |
| 2026-08-23 | M4-C / [PR #29](https://github.com/JapmeetBrar/pufferlab/pull/29) | `implementing → review_requested` | Repair `85348fd` uses platform atomic exchange and no-replace primitives, inspects the displaced authenticated inode/bytes before commit, restores substitutions without clobbering them, anchors coordination on the independently held OS-account home directory, revalidates every fixed-chain and lock identity before destructive/provider actions, and rejects FIFO/socket/device/nonregular fixed children through nonblocking opens. The exact reviewer attacks pass 119 focused tests and ten consecutive repetitions; `make check` passes 776 backend tests plus one opt-in skip and 49 frontend tests; generated TypeScript is drift-free; artifact audit passes `245/747/19`; synthetic privacy passes `245/747/16,427,726/25`; and `git diff --check` is clean. Only private test paths, fake/adversarial providers, and marker credentials were used. Independent exact-head re-review is next. |
| 2026-08-23 | M4-C / [PR #29](https://github.com/JapmeetBrar/pufferlab/pull/29) | `review_requested → changes_requested → implementing` | [Independent review at `30da4a3`](https://github.com/JapmeetBrar/pufferlab/pull/29#issuecomment-5388198302) reproduced three blockers: receipt swap races can make unconditional replace/unlink clobber a substitute, replacing the lock or relocating the state directory can split coordination, and blocking FIFO opens can hang fixed-child inspection. The repair must use identity-conditional same-directory exchange/restore semantics, a stable OS-account-rooted coordination anchor with continuity checks, nonblocking no-follow opens for every fixed child, and exact provider-free adversarial tests before re-review. |
| 2026-08-23 | M4-C / [PR #29](https://github.com/JapmeetBrar/pufferlab/pull/29) | `implementing → review_requested` | Focused implementation `e17a2e9` was committed before normally merging verified M4-D main `ac78452` as merge head `74cb357`. The merged head passes 105 focused tests; `make check` with 762 backend tests plus one opt-in skip and 49 frontend tests; generated TypeScript drift; dataset artifact audit over 245 current files, 728 history blobs, and 19 ignored paths; synthetic secret-boundary audit over 245 tracked files, 728 history blobs, 15,526,522 bytes, and 25 browser files; and `git diff --check`. Only fake/adversarial providers and marker credentials were used. Independent exact-head review and reviewer-only merge are next. |
| 2026-08-23 | M4-D / [PR #27](https://github.com/JapmeetBrar/pufferlab/pull/27) | `review_requested → approved → merged → verified` | [Independent exact-head review](https://github.com/JapmeetBrar/pufferlab/pull/27#issuecomment-5388111130) approved pure gate head `dbe6ecf` after focused, full, generated, artifact, privacy, purity, and exact-head CI gates. The reviewer-only squash merge produced `ac78452`; the [canonical protected-main record](https://github.com/JapmeetBrar/pufferlab/pull/27#issuecomment-5388121607) confirms fresh Backend and Frontend checks passed. |
| 2026-08-23 | M4-B / [PR #26](https://github.com/JapmeetBrar/pufferlab/pull/26) | `review_requested → approved → merged → verified` | [Independent final review](https://github.com/JapmeetBrar/pufferlab/pull/26#issuecomment-5388026484) approved exact head `586a190` after the portable in-memory SQLite snapshot repair and complete local/Ubuntu gates. The reviewer-only squash merge produced `82e2f59`; the [canonical protected-main record](https://github.com/JapmeetBrar/pufferlab/pull/26#issuecomment-5388034421) confirms fresh Backend and Frontend checks passed. |
| 2026-08-23 | M4-C / `codex/m4-owned-tiny` | `planned → assigned → implementing` | The namespace lifecycle worker started from verified M4-B base `82e2f59`. The focused review unit owns only the OS-account-fixed authenticated owner key/receipt, generated tiny ingest integration, receipt-backed `namespace show-tiny`/`cleanup-tiny`, and fake/adversarial lifecycle tests. It must prove durable intent, HMAC-derived exact authority, no-follow modes and locking, authenticated atomic CAS/fsync transitions, credential/creating-region binding, finite resume/cleanup, bounded absence verification, retained recovery receipts, and zero caller target/path/token seams without reading a real key or reaching turbopuffer. |
| 2026-08-22 | LOOP / [PR #3](https://github.com/JapmeetBrar/pufferlab/pull/3) | `review_requested → changes_requested → implementing → review_requested → approved → merged → verified` | Dedicated review found and re-reviewed workflow blockers. The corrected policy and active ruleset `21190317` were approved; PR #3 merged as `78d5ddf` with required checks green. |
| 2026-08-22 | M1-A / [PR #5](https://github.com/JapmeetBrar/pufferlab/pull/5) | `review_requested → changes_requested → implementing → review_requested → changes_requested → implementing → review_requested → approved → merged → verified` | Review at `7ec8221` found retained secret-bearing exception context and a stale ledger; the worker fixed both at `2baa4a1`. First re-review verified those fixes, then found that `FullTextSearchSchema` omitted frozen, provider-supported `k3`. Head `6289037` aligned the typed boundary and exact fake/live write shapes; dedicated re-review approved it, and PR #5 merged as `23ccd79` with protected checks green. |
| 2026-08-22 | M1-B / [PR #4](https://github.com/JapmeetBrar/pufferlab/pull/4) | `review_requested → changes_requested → implementing → review_requested → changes_requested → implementing → review_requested → approved → merged → verified` | First review found strict JSON/canonicalization gaps, an imprecise schema boundary, count-only readiness, non-finite vectors, retained error details, and stale evidence; head `febee94` repaired those and integrated the merged typed provider. [Second re-review](https://github.com/JapmeetBrar/pufferlab/pull/4#issuecomment-5379439529) found that readiness still used local acknowledgements and approximate metadata, SDK optional noise was not normalized, the observed ANN metric was injected rather than read, and the FTS byte limit was one too high. Head `71bfc6a` added independently observed strong ordered IDs and exact Count aggregation, real-SDK metadata normalization, observed metric/schema drift checks, and the correct 254-byte FTS limit. Dedicated re-review approved it; PR #4 merged as `7086d2b` and protected post-merge checks passed. |
| 2026-08-22 | M1-C / [PR #6](https://github.com/JapmeetBrar/pufferlab/pull/6) | `implementing → review_requested → changes_requested → implementing → review_requested → changes_requested → implementing → review_requested → approved → merged → verified` | The worker normally merged M1-B from protected `main`, integrated the exact manifest and compiled schema hash without casts, and added the production-shaped BM25/ANN compare service and API. [Initial review](https://github.com/JapmeetBrar/pufferlab/pull/6#issuecomment-5379587091) found that filter overrides lacked schema-bound validation. The first repair added contract and service validation; [first re-review](https://github.com/JapmeetBrar/pufferlab/pull/6#issuecomment-5379634560) verified it but found that `RuntimeSearchBackend` checked missing credentials before constructing the validated service. The second repair validates with the same stored compiled write spec before `_get_service`; dedicated re-review approved it, PR #6 merged as `5ca296c`, and protected post-merge checks passed. No live credential was consumed; the isolated provider live test remains opt-in. |
| 2026-08-22 | M1-D / [PR #8](https://github.com/JapmeetBrar/pufferlab/pull/8) | `planned → assigned → implementing → review_requested → approved → merged → verified` | The worker based the UI on merged compare API commit `5ca296c`, normally merged protected `main` at `a5442f9`, and used only generated OpenAPI types for browser domain data. [Dedicated review](https://github.com/JapmeetBrar/pufferlab/pull/8#issuecomment-5379781262) verified the exact request/response/error boundary, stable URL, safe links/rendering, responsive/accessibility states, contrast, and exposure scans. PR #8 squash-merged as `b7ca478`; protected post-merge checks passed. Successful result rendering is fake-contract tested; live search remains the active M1-F verification step. |
| 2026-08-22 | M1-E / [PR #7](https://github.com/JapmeetBrar/pufferlab/pull/7) | `implementing → review_requested → approved → merged → verified` | The worker wired the checked-in 20-document fixture to the exact pinned BGE passage embedder and merged ingestion/provider services behind one injectable CLI command. [Dedicated review](https://github.com/JapmeetBrar/pufferlab/pull/7#issuecomment-5379745253) independently verified the installed entrypoint, pinned lazy model path, owned namespace/readiness and idempotence boundaries, redacted failures, and the no-deletion policy. PR #7 squash-merged as `a5442f9` and protected post-merge checks passed. No credential or model download was consumed; live vertical-slice verification remains pending. |
| 2026-08-22 | M1-F / [draft PR #9](https://github.com/JapmeetBrar/pufferlab/pull/9) | `implementing → review_requested → changes_requested → implementing → review_requested → changes_requested → implementing → review_requested` | [Early review at `bced42b`](https://github.com/JapmeetBrar/pufferlab/pull/9#issuecomment-5381764970) found the skipped-success credential path plus API, secret, cleanup, and evidence blockers. [Review at `d98b25a`](https://github.com/JapmeetBrar/pufferlab/pull/9#issuecomment-5382354782) requested stronger result binding and exit/signal propagation; [review at `70e5e34`](https://github.com/JapmeetBrar/pufferlab/pull/9#issuecomment-5382623833) accepted those repairs and authorized the credentialed run. Live diagnostics exposed only the provider-owned system `id:string` schema entry; normalization head `c8dd6d0` was [independently accepted](https://github.com/JapmeetBrar/pufferlab/pull/9#issuecomment-5382722368). Timing head `9293155` was [independently accepted](https://github.com/JapmeetBrar/pufferlab/pull/9#issuecomment-5382741571). The completed live run then exposed a Zsh-only second cleanup attempt after already-confirmed deletion; the runbook now disarms the restored outer trap at top level. All sanitized evidence is recorded and the final exact head awaits review. |
| 2026-08-22 | M1-F / [PR #9](https://github.com/JapmeetBrar/pufferlab/pull/9) | `review_requested → approved → merged → verified` | [Dedicated final review](https://github.com/JapmeetBrar/pufferlab/pull/9#issuecomment-5382791048) reproduced the Bash/Zsh cleanup behavior, reran full gates, and found no blocker. PR #9 merged as `c1fa093`; [post-merge evidence](https://github.com/JapmeetBrar/pufferlab/pull/9#issuecomment-5382795239) records green Backend and Frontend checks on protected `main`. |
| 2026-08-22 | M2-0 / [PR #10](https://github.com/JapmeetBrar/pufferlab/pull/10) | `planned → assigned → implementing → review_requested` | Root created the coordination branch from `c1fa093`, recorded the finite task graph, and passed 163 backend tests plus one opt-in skip and six frontend tests with all static/build/contract checks clean. M2-A through M2-D must branch only after dedicated review and merge. |
| 2026-08-22 | M2-0 / [PR #10](https://github.com/JapmeetBrar/pufferlab/pull/10) | `review_requested → changes_requested → implementing` | [Dedicated review](https://github.com/JapmeetBrar/pufferlab/pull/10#issuecomment-5383830840) found the technical graph clean but required an exact CQADupStack revision/checksum and auditable license/NOTICE/tracked-artifact policy. Root verified the official BEIR registry and original dataset repository sources, stopped a nonessential 5.34 GB checksum download, moved the recoverable partial file to Trash, and implemented the source/license boundary for re-review. |
| 2026-08-22 | M2-0 / [PR #10](https://github.com/JapmeetBrar/pufferlab/pull/10) | `implementing → review_requested` | Repair head `ec0809e` records the exact BEIR/CQADupStack acquisition revisions, published archive MD5 plus mandatory completed-download SHA-256 lock, Apache-2.0 versus CC BY-SA license chain, retained attribution audit, and exhaustive tracked/ignored inventory. `git diff --check` is clean; exact-head GitHub gates and dedicated re-review are next. |
| 2026-08-22 | M2-0 / [PR #10](https://github.com/JapmeetBrar/pufferlab/pull/10) | `review_requested → approved → merged → verified` | [Dedicated re-review at `811febf`](https://github.com/JapmeetBrar/pufferlab/pull/10#issuecomment-5383856977) closed the provenance/licensing blocker and approved the exact repaired head. PR #10 squash-merged as `42c3da5a912c565898ecba2c5e4504d4395994db`; the [canonical post-merge record](https://github.com/JapmeetBrar/pufferlab/pull/10#issuecomment-5383860571) confirms fresh protected `main` at that commit with [Backend](https://github.com/JapmeetBrar/pufferlab/actions/runs/32613629035/job/97130560883) and [Frontend](https://github.com/JapmeetBrar/pufferlab/actions/runs/32613629035/job/97130561087) successful. |
| 2026-08-22 | M2-A | `planned → assigned → implementing` | eval_worker branched `codex/m2-eval-engine` from reviewed M2-0 merge `42c3da5` and owns only the pure evaluation engine, focused tests, and this task ledger entry. |
| 2026-08-22 | M2-A / [PR #11](https://github.com/JapmeetBrar/pufferlab/pull/11) | `implementing → review_requested` | Implementation commit `ffba8f5` defines graded exponential-gain NDCG@10, positive-qrel Recall@50/MRR@10, documented type-7 percentiles, failure-safe aggregation, explicit pairing states, candidate-minus-baseline deltas, and contract-ordered regression/gain subsets. Forty focused tests cover hand-calculated and independent exhaustive reference results, exact cutoffs, empty/tied/duplicate/missing/no-positive-qrel cases, finite-value boundaries, sample counts, failures, and package import purity. Standard gates pass with 203 backend tests plus one opt-in skip and six frontend tests; exact-head CI and dedicated review are next. |
| 2026-08-22 | M2-A / [PR #11](https://github.com/JapmeetBrar/pufferlab/pull/11) | `review_requested → changes_requested → implementing` | [Independent review at `6908c6f`](https://github.com/JapmeetBrar/pufferlab/pull/11#issuecomment-5383902407) found no evaluation-code blocker and independently matched 4,999 randomized graded/duplicate rankings plus a type-7 percentile grid. The sole blocker was stale M2-0 status in this first planned post-merge ledger update; eval_worker began the bounded documentation-only repair. |
| 2026-08-22 | M2-A / [PR #11](https://github.com/JapmeetBrar/pufferlab/pull/11) | `implementing → review_requested` | The documentation-only repair marks M2-0 verified and records its actual independent approval, squash merge `42c3da5a912c565898ecba2c5e4504d4395994db`, and successful protected-main Backend/Frontend checks. Evaluation code is unchanged; `git diff --check` and direct GitHub ledger status/link inspection pass. Dedicated re-review of the exact new PR head is next. |
| 2026-08-22 | M2-A / [PR #11](https://github.com/JapmeetBrar/pufferlab/pull/11) | `review_requested → approved → merged → verified` | [Dedicated re-review](https://github.com/JapmeetBrar/pufferlab/pull/11#issuecomment-5383928856) verified the ledger-only repair and unchanged evaluation engine. PR #11 squash-merged as `7062d6b5fc27d6d7ae525967e3dbf7f60080966c`; the [canonical record](https://github.com/JapmeetBrar/pufferlab/pull/11#issuecomment-5383931950) confirms fresh protected-main [Backend](https://github.com/JapmeetBrar/pufferlab/actions/runs/32614535686/job/97132946084) and [Frontend](https://github.com/JapmeetBrar/pufferlab/actions/runs/32614535686/job/97132946182) passed. |
| 2026-08-22 | M2-C / `codex/m2-hybrid-retrieval` | `planned → assigned → implementing` | Root retrieval worker branched from reviewed M2-0 merge `42c3da5`, verified the installed turbopuffer 2.9 multi-query/RRF boundary against current official docs, and began the focused provider/retrieval implementation. |
| 2026-08-22 | M2-C / [PR #13](https://github.com/JapmeetBrar/pufferlab/pull/13) | `implementing → review_requested → changes_requested → implementing` | Implementation head `3f6d906` passed 76 focused tests plus one opt-in skip, 181 full backend tests plus one opt-in skip, strict static/OpenAPI checks, and the full frontend gate. [Independent review at `821ed7a`](https://github.com/JapmeetBrar/pufferlab/pull/13#issuecomment-5383952134) reproduced cancellation-driven concurrent cached-model use and a provider lifecycle leak, found that hybrid execution collapsed frozen title/body lexical weights, required tie-safe live parity, and blocked the stale conflicting base. Root merged protected `main` at `7062d6b` and began the bounded repair. |
| 2026-08-22 | M2-B | `planned → assigned → implementing` | Persistence worker accepted the focused branch from reviewed M2-0 merge `42c3da5`, read the workflow and persistence/job contracts, and began the SQLite revision, outcome, migration, recovery, and cancellation implementation. Evaluation math, provider retrieval, dataset acquisition, CLI routing, and UI remain out of scope. |
| 2026-08-22 | M2-B / [PR #12](https://github.com/JapmeetBrar/pufferlab/pull/12) | `implementing → review_requested` | Implementation head `c172eef` passes 19 focused tests, 182 full backend tests plus one opt-in live skip, six frontend tests, Ruff, formatting, mypy, OpenAPI/generated-TypeScript drift, lint, typecheck, production build, clean migration upgrade/downgrade and metadata comparison, packaged-migration inspection, `git diff --check`, ignore proof, and staged secret/artifact scans. The branch changes only persistence/jobs, the initial migration, dependency/config declarations, tests, and this ledger; the dedicated reviewer must inspect and merge. |
| 2026-08-22 | M2-B / [PR #12](https://github.com/JapmeetBrar/pufferlab/pull/12) | `review_requested → changes_requested → implementing` | [Dedicated review](https://github.com/JapmeetBrar/pufferlab/pull/12#issuecomment-5383926693) reproduced non-UTC canonical payloads, queued-run interruption, summary-less terminal completion, and nondeterministic loss of a successful peer outcome when another simultaneous task failed. The worker repaired all four code blockers and ledger defects before rerunning focused concurrency repetitions and full gates. |
| 2026-08-22 | M2-B / [PR #12](https://github.com/JapmeetBrar/pufferlab/pull/12) | `implementing → review_requested` | Repair head `f94fd8a` passes 22 focused tests, 400 repeated synchronized peer-failure iterations plus an independent 40-run real-repository probe with zero lost successes, 185 full backend tests plus one opt-in live skip, and six frontend tests. Ruff, formatting, mypy, OpenAPI/generated-TypeScript drift, lint, typecheck, build, migration/model parity, packaged resources, `git diff --check`, ignore proof, and secret/artifact scans are clean. The repaired ledger also records M2-0's canonical verified transition without breaking the Markdown table. |
| 2026-08-22 | M2-B / [PR #12](https://github.com/JapmeetBrar/pufferlab/pull/12) | `review_requested → review_requested` (base refresh) | Protected `main` advanced through verified M2-A PR #11 while repair review was pending. The worker normally merged `origin/main` as `efff95b`, resolved only the shared ledger by retaining both canonical task histories, carried M2-A to verified, and reran integrated gates: 225 backend tests plus one opt-in skip and six frontend tests, with all static, contract, and build checks clean. |
| 2026-08-22 | M2-B / [PR #12](https://github.com/JapmeetBrar/pufferlab/pull/12) | `review_requested → approved → merged → verified` | [Independent re-review](https://github.com/JapmeetBrar/pufferlab/pull/12#issuecomment-5383977502) closed every persistence blocker at `ef6a998` after 22 focused tests, 400 repeated two-task races, and 100 four-way SQLite races. PR #12 squash-merged as `55a72410b241b9baa46899448a829de2b6b9e130`; the [canonical record](https://github.com/JapmeetBrar/pufferlab/pull/12#issuecomment-5383980690) confirms fresh protected-main [Backend](https://github.com/JapmeetBrar/pufferlab/actions/runs/32615143289/job/97134520550) and [Frontend](https://github.com/JapmeetBrar/pufferlab/actions/runs/32615143289/job/97134520625) passed. |
| 2026-08-22 | M2-C / [PR #13](https://github.com/JapmeetBrar/pufferlab/pull/13) | `implementing → review_requested` | Repair `2ae0a5a` adds deterministic cancellation/concurrency and partial-construction cleanup regressions, compiles `Sum(Product(title), Product(body))` identically for standalone and hybrid lexical retrieval, verifies the exact JSON shape through the real turbopuffer SDK, defines zero-weight behavior and identity changes, and compares live RRF tie groups without assuming the undocumented server tie-break. After normally merging verified M2-B `main` as `b0c49ef`, the integrated branch passes 247 backend tests plus one opt-in live skip, six frontend tests, Ruff, formatting, mypy, OpenAPI drift, lint, typecheck, and production build. Dedicated re-review must inspect and merge. |
| 2026-08-22 | M2-C / [PR #13](https://github.com/JapmeetBrar/pufferlab/pull/13) | `review_requested → approved → merged → verified` | [Independent re-review at `b79bb5c`](https://github.com/JapmeetBrar/pufferlab/pull/13#issuecomment-5384004506) closed the cancellation, lifecycle, weighted-lexical, identity, and tie-safe parity blockers. PR #13 reviewer-only squash-merged as `48f4e291a5c53a557b1d3ce9455098ea80bb394b`; the [canonical record](https://github.com/JapmeetBrar/pufferlab/pull/13#issuecomment-5384007820) confirms fresh protected-main [Backend](https://github.com/JapmeetBrar/pufferlab/actions/runs/32615462659/job/97135346178) and [Frontend](https://github.com/JapmeetBrar/pufferlab/actions/runs/32615462659/job/97135346070) passed. |
| 2026-08-22 | M2-D | `planned → assigned → implementing` | Dataset worker branched `codex/m2-unix-dataset` from reviewed M2-0 merge `42c3da5`, normally merged verified M2-B protected `main` at `55a7241`, and then normally merged verified M2-C protected `main` at `48f4e29` while retaining canonical task histories. Work remains confined to deterministic dataset adapter/manifests, attribution/audit documentation, dataset tests, and resumable ingestion service extensions; real licensed text and the 5.34 GB archive remain local and ignored. |
| 2026-08-22 | M2-D | `implementing → review_requested` | The pinned 5,343,728,040-byte official archive passed the published MD5 before the locally recorded completed-download SHA-256. Two real preparations produced content address `6d54fb92c04b9f193d081a7c430d8804e24e71855d3cbaa2bb50cde838f181b8`; the ID-only curation verified, and the loader retained 47,382 documents, 50 curated queries, and 83 judgments with corpus hash `14299866b2762f502de864baa5060d1b633bad1b1ccec2cc0898c1c92990ca24`. The exact generated 5.34 GB archive directory and task scratch artifacts were deleted after validation; no real text or personal metadata entered Git. Fourteen focused tests, 239 full backend tests plus one opt-in skip, six frontend tests/build, static/contract checks, and current/history/ignore audits pass. Dedicated review must independently inspect and merge. |
| 2026-08-22 | M2-D | `review_requested → changes_requested → implementing` | A pre-PR M2-E integration audit found that the compatibility `FixtureQuery` view flattened qrel grades, curation reasons were not exposed through a seed boundary, and M2-E would have had to duplicate pack/checkpoint/resume orchestration. The repair adds lossless local-pack qrels, stable contract-native `DatasetVersion`/`QuerySet`/`JudgedQuery` materialization with official grades and parallel tag/reason metadata, direct SQLite round-trip coverage, and a no-delete `UnixDatasetApplicationService` that returns the ready seed after checkpointed readiness. |
| 2026-08-22 | M2-D / [PR #14](https://github.com/JapmeetBrar/pufferlab/pull/14) | `implementing → review_requested` | After normally merging verified M2-C `main` at `48f4e29` as integration merge `83efcff`, the repaired focused suite passes 16 tests: qrel grades and stable document UUIDs survive deterministic materialization, authored primary/all tags and reasons remain exposed, contract-native seeds round-trip through SQLite, and a second application-service run resumes all 60 synthetic documents without another upsert or delete surface. Full gates pass with 263 backend tests plus one opt-in live skip and six frontend tests/build; Ruff, formatting, strict mypy, OpenAPI/generated-TypeScript drift, diff checks, and the current/history/ignore artifact audit are clean. Dedicated review must inspect the entire checksum/license/processing/ingestion diff and merge only after exact-head GitHub checks pass. |
| 2026-08-22 | M2-D / [PR #14](https://github.com/JapmeetBrar/pufferlab/pull/14) | `review_requested → changes_requested → implementing` | [Independent review at `8808825`](https://github.com/JapmeetBrar/pufferlab/pull/14#issuecomment-5384062410) reproduced that public loaders trusted self-declared archive/preprocessing/content hashes and an unbound directory name, including row drift hidden by a self-updated manifest. It also proved the required GitHub Backend check used a depth-1 checkout and scanned only 159 current/history blobs instead of the full checkout's 345 historical blobs. The repair will bind the checked source lock and every derived content identity before row access, reject missing/extra relevant pack files, fetch full CI history, and make shallow-history audit claims fail closed. |
| 2026-08-22 | M2-D / [PR #14](https://github.com/JapmeetBrar/pufferlab/pull/14) | `implementing → review_requested` | The repair adds checked `processed-pack-lock.json` identity, strict source/archive/preprocessing/record binding, independently recomputed exact file hashes/counts and content address, exact directory inventory/basename checks, and retained-row content-hash validation. Negative tests reject every reported manifest mutation, missing/extra files, wrong directories, and changed rows with self-updated hashes/manifest/directory; the public loader and application path verify before materialization. Backend CI fetches full history and a shallow audit now fails closed. Twenty-six focused tests and 273 full backend tests plus one opt-in skip pass; six frontend tests/build, Ruff/format/mypy/OpenAPI/generated-TypeScript/diff gates, wheel forbidden-artifact/credential-pattern inspection, a synthetic exact secret-boundary scan, and the local current/history/ignore audit (`160/346/19`) are clean. The temporary wheel directory was moved to Trash after inspection. Exact-head GitHub checks and independent re-review remain required. |
| 2026-08-22 | M2-D / [PR #14](https://github.com/JapmeetBrar/pufferlab/pull/14) | `review_requested → approved → merged → verified` | [Independent re-review at `7187702`](https://github.com/JapmeetBrar/pufferlab/pull/14#issuecomment-5384120560) reproduced rejection of the original manifest attack, a self-consistent rows/files/manifest/directory rewrite, and a shallow-clone history audit. PR #14 reviewer-only squash-merged as `31387b6b62cc938a3b0d107c4c4ac5e0fd7fb4c9`; the [canonical record](https://github.com/JapmeetBrar/pufferlab/pull/14#issuecomment-5384124747) confirms fresh protected-main Backend and Frontend passed with full-history audit `160/357/19`. |
| 2026-08-22 | M2-E / `codex/m2-eval-cli` | `planned → assigned → implementing` | Root branched from verified M2-D protected `main` `31387b6`. Retrieval/config, durable evaluation orchestration, and CLI/export seams are delegated with disjoint file ownership; integration must preserve the public 2–4 config compare contract, bind persisted configs to the namespace-specific dataset revision, seed result depth 50, and persist before progress. |
| 2026-08-22 | M2-E / `codex/m2-eval-cli` | `implementing → changes_requested → implementing` | Dedicated slice review reproduced that `BoundSearchCatalog` trusted only ID/name/mode/hash summaries, allowing result depth and lexical provider semantics to drift behind an unchanged persisted identity. Repair `9672825` derives every executable provider field from fully revalidated immutable configs plus the bound dataset/manifest, removes raw-catalog runtime injection, and rejects stale identity, namespace, manifest, vector, RRF, and reranker mismatches. Independent re-review approved the exact repair after an adversarial field matrix and 54 focused tests. |
| 2026-08-22 | M2-E / [PR #15](https://github.com/JapmeetBrar/pufferlab/pull/15) | `implementing → review_requested` | Implementation head `05692d8` wires the default CLI to migrations, deterministic seed selection, Unix checkpointed ingestion, exact bound retrieval, 50×4 durable scheduling, post-commit progress, redacted coverage failures, cancellation/drain, and typed partial export; a follow-up cancellation shield regression is included in the handoff head. Local gates pass with 318 backend tests plus one opt-in live skip, six frontend tests/build, Ruff, format, strict mypy, OpenAPI drift, `git diff --check`, and full-history dataset audit `172/387/19`. Synthetic production-composition coverage runs and exports all 200 attempts and closes both provider boundaries. No live credential, licensed pack, raw vector, SQLite file, or export entered Git. Exact-head CI and dedicated whole-branch review are next. |
| 2026-08-22 | M2-E / [PR #15](https://github.com/JapmeetBrar/pufferlab/pull/15) | `review_requested → changes_requested → implementing` | [Independent whole-branch review at `7e6593b`](https://github.com/JapmeetBrar/pufferlab/pull/15#issuecomment-5384234794) passed 318 backend tests plus one opt-in skip, six frontend tests, static/contract/build checks, artifact audit, and exact-head GitHub CI, but reproduced two P1 blockers. A query set bound to a second valid dataset passed seed validation and left partial persisted records before later run rejection; interrupted CLI execution always printed `status=cancelled` even when durable drain returned `FAILED` or drain/close raised. The worker returned the task to implementation and is adding pre-write identity validation plus honest, redacted interrupt cleanup reporting. |
| 2026-08-22 | M2-E / [PR #15](https://github.com/JapmeetBrar/pufferlab/pull/15) | `implementing → review_requested` | Repair `3017896` rejects a cross-bound query set before persisting the seeded dataset, configs, or queries, with an adversarial second-valid-dataset/no-write regression. Interrupt handling now preserves exit 130 but prints the returned durable status, or `status=interrupt_requested cleanup=failed` when cancellation/drain state is unknown; paired tests cover durable `FAILED` and simultaneous redacted drain/close failures. `make check` passes with 321 backend tests plus one opt-in skip and six frontend tests/build; Ruff, format, strict mypy, OpenAPI drift, `git diff --check`, and full-history audit `172/403/19` pass. Exact-head CI and dedicated re-review must independently verify both blockers before merge. |
| 2026-08-22 | M2-E / [PR #15](https://github.com/JapmeetBrar/pufferlab/pull/15) | `review_requested → approved → merged → verified` | [Dedicated re-review](https://github.com/JapmeetBrar/pufferlab/pull/15#issuecomment-5384253856) independently proved cross-dataset rejection before writes and honest durable `FAILED`/simultaneous cleanup-failure interrupt output at exit 130. The reviewer squash-merged PR #15 as `0e91e2bd24194e375e0dd5d6b0e12b729a3409d0`; the [canonical protected-main record](https://github.com/JapmeetBrar/pufferlab/pull/15#issuecomment-5384257932) confirms Backend and Frontend checks passed. |
| 2026-08-22 | M2-F / `codex/m2-live-finalization` | `planned → assigned → implementing` | Root branched from verified M2-E protected `main` `0e91e2b`. The live_harness worker owns only the exact generated-namespace cleanup capability, read-only persisted-summary verifier, and focused adversarial tests; root owns the runbook, ignored credentialed execution, artifact scans, sanitized evidence, and final handoff. No credential or remote namespace is used until the safety harness receives independent pre-live review. |
| 2026-08-22 | M2-F / [draft PR #16](https://github.com/JapmeetBrar/pufferlab/pull/16) | `implementing → review_requested` (pre-live safety gate) | Worker commit `6fbfa3f` adds an internally generated `pufferlab-unix-live-<24 lowercase hex>` capability in an exclusive no-follow mode-0600 ignored record, target-free typed-`NOT_FOUND` cleanup, close/record rechecks, and a UUID-only read-only SQLite verifier. Root head `1bfd453` additionally requires all 200 outcomes to succeed with six 50-sample metrics and zero error rate, and documents recoverable traps through exact cleanup. Seventeen focused adversarial tests cover target/shape/mode/symlink/record-swap rejection, delete/poll/close record retention, self-consistent failure rejection, summary/coverage drift, read-only bytes, and redacted output. `make check` passes with 338 backend tests plus one opt-in skip and six frontend tests/build; static/contract checks, full-history audit `176/417/19`, exact secret/browser scan, and `git diff --check` pass. The reviewer must inspect and test this gate without merging; no live network call or namespace has occurred. |
| 2026-08-22 | M2-F / [draft PR #16](https://github.com/JapmeetBrar/pufferlab/pull/16) | `review_requested → changes_requested → implementing` (pre-live safety gate) | [Independent review at `0956577`](https://github.com/JapmeetBrar/pufferlab/pull/16#issuecomment-5384317895) passed 17 focused tests, 338 backend tests plus one skip, six frontend tests/build, read-only byte/mtime/sidecar checks, trap and failure-retention attacks, artifact/secret audits, and exact-head CI, but reproduced four P1 blockers. Caller path/token seams could authorize a hand-authored valid-prefix foreign target; session creation lacked file/directory `fsync`; the verifier accepted a different failed one-document dataset and noncanonical BM25 settings; and self-consistent stored `.25` query metrics passed despite a perfect ranking/qrel. Credentialed execution remains unauthorized while root adds fixed-path authenticated derivation, durable persistence, exact checked dataset/config/session binding, and independent ranking/qrel metric recomputation. |
| 2026-08-22 | M2-F / [draft PR #16](https://github.com/JapmeetBrar/pufferlab/pull/16) | `implementing → review_requested` (pre-live safety gate) | Repair `12b0295` derives the only cleanup target from a fixed-path mode-`0600` owner key plus an internal nonce, authenticates the fixed session record, durably `fsync`s ownership-file and directory creation, and leaves no production path/key/token/target seam. The UUID-only verifier authenticates that session, rebuilds the exact content-addressed 47,382-document Unix seed with 50 curated queries and 83 qrels, derives the canonical BM25/ANN/server-RRF/local-reranker catalog, and recomputes every quality metric from ranked IDs and qrels before independent aggregation. Twenty-three focused tests include every prior exploit; `make check` passes with 344 backend tests plus one opt-in skip and six frontend tests/build, static/contract checks, artifact audit `176/419/19`, secret-boundary verification, direct CLI entrypoints, and `git diff --check`. Dedicated exact-head re-review must approve before any credentialed network call. |
| 2026-08-22 | M2-F / [draft PR #16](https://github.com/JapmeetBrar/pufferlab/pull/16) | `review_requested → approved → implementing` (pre-live safety gate) | [Dedicated re-review at `fea5ace`](https://github.com/JapmeetBrar/pufferlab/pull/16#issuecomment-5384384645) independently rejected foreign and hand-authored ownership records, observed owner/session file-then-directory `fsync`, rebuilt the exact READY 47,382-document/50-query/83-qrel/four-config suite, rejected the prior dataset/config/metric attacks, and proved read-only hash/mtime/sidecar stability. Focused 23-test and full 344-plus-one-skip backend suites, six frontend tests/build, static/contract/audit/redaction gates, and exact-head GitHub checks passed. The limited credentialed runbook execution is authorized; no live call occurred during review. |
| 2026-08-22 | M2-F / [draft PR #16](https://github.com/JapmeetBrar/pufferlab/pull/16) | `implementing → implementing` (live readiness repair) | The first authorized run verified the real pinned BGE model and passed the separate self-cleaning BM25/ANN/server-RRF provider smoke test. Unix ingestion then checkpointed all 741 batches and 47,382 stable document IDs, but the final inventory path issued one `top_k=47,383` request against the provider's documented 10,000 maximum and failed closed before persisting a READY seed or starting evaluation. The coordinator trap confirmed exact namespace deletion to `NOT_FOUND`; the authenticated session record is absent, its mode-`0600` ignored owner key remains, and no tracked file changed. Repair is limited to strong-consistency ID-ordered keyset pagination capped at 10,000 rows per query, adversarial tests, full gates, and independent pre-retry review. |
| 2026-08-22 | M2-F / [draft PR #16](https://github.com/JapmeetBrar/pufferlab/pull/16) | `implementing → review_requested` (live readiness repair) | Worker repair `9e4cdfb` preserves the exact count-plus-one inventory contract while paging at most 10,000 strongly consistent ascending IDs and advancing an exclusive `id > last_id` cursor. Tests cover the exact 47,382 corpus shape, the 10,001st-row truncation proof, an exact-full-page empty successor, cursor/consistency arguments, mixed or invalid IDs, duplicates/out-of-order/non-progress, oversized pages, and short-page/count inconsistency. Fifty-three focused tests pass; `make check` passes with 354 backend tests plus one opt-in skip and six frontend tests/build, Ruff/format/mypy/OpenAPI/diff gates pass, and artifact `176/428/19` plus secret-boundary audits are clean. Dedicated exact-head review is required before retry. |
| 2026-08-22 | M2-F / [draft PR #16](https://github.com/JapmeetBrar/pufferlab/pull/16) | `review_requested → approved → implementing` (live readiness repair) | [Dedicated review at `d07582a`](https://github.com/JapmeetBrar/pufferlab/pull/16#issuecomment-5384486966) reproduced the original over-limit request and used the real SDK offline to verify five strong keyset pages bounded to 10,000 with exact cursors. All boundary/attack tests passed; the absent session, retained mode-`0600` owner key, zero application-table rows, unchanged read-only database, clean worktree, full gates/audits, and exact-head GitHub Backend/Frontend checks were independently confirmed. The runbook-scoped credentialed retry is authorized; the draft remains unmerged. |
| 2026-08-22 | M2-F / [draft PR #16](https://github.com/JapmeetBrar/pufferlab/pull/16) | `implementing → review_requested` (goal-finalization review) | The authorized retry passed five-page exact readiness for all 47,382 documents, persisted dataset `dd403bda-d298-5a4a-bb45-a29dee84c61b`, query set `e490c239-69ee-57d6-b6b0-bb5cc31c8b2a`, four canonical configs, run `34af9297-e423-4787-a43b-37292aaf1d93`, and 200/200 successful outcomes. The authenticated verifier recomputed every quality metric from rankings and 83 exact qrels, matched all six 50-sample aggregates per config, validated export SHA-256 `46b175d9cd88e2f102876c00c5e3a60e533c8dd0cea07118b215efef0ba83e02`, and remained byte/mtime/sidecar read-only. Namespace fingerprint `505a2788e77e` reached exact `NOT_FOUND`; the session is absent and ignored owner key remains mode `0600`. Full gates pass with 354 backend tests plus one skip and six frontend tests/build; artifact `176/431/19`, secret-boundary, diff, and post-cleanup Bash gates pass. Final exact-head reviewer-only merge and protected-main verification remain. |
| 2026-08-22 | M2-F / [PR #16](https://github.com/JapmeetBrar/pufferlab/pull/16) | `review_requested → approved → merged → verified` | [Dedicated final review](https://github.com/JapmeetBrar/pufferlab/pull/16#issuecomment-5384603898) matched the persisted IDs, 50/200 coverage, metrics, export hash, cleanup, and artifact boundaries. PR #16 reviewer-only squash-merged as `a1f1197bbd600800f513b7079edb57629c3ba955`; the [canonical closing record](https://github.com/JapmeetBrar/pufferlab/pull/16#issuecomment-5384608895) confirms protected-main Backend and Frontend checks passed. |
| 2026-08-23 | M3-0 / [PR #17](https://github.com/JapmeetBrar/pufferlab/pull/17) | `planned → assigned → implementing → review_requested` | Root based the coordination branch on verified Milestone 2 merge `a1f1197`, then delegated read-only backend and frontend audits. The plan chooses provider-free SQLite dashboard reads plus explicit dataset-bound live replay, resolves cancellation/canonical-suite/original-evidence contradictions, defines six finite review units and rollback boundaries, and keeps trace persistence/config editing/distributed execution out of P0. `make check` passes with 354 backend tests plus one opt-in skip and six frontend tests/build; Ruff, format, mypy, OpenAPI drift, full-history artifact audit `177/433/19`, and `git diff --check` pass. Dedicated plan review and merge are required before M3-A starts. |
| 2026-08-23 | M3-0 / [PR #17](https://github.com/JapmeetBrar/pufferlab/pull/17) | `review_requested → changes_requested → implementing` | [Independent review](https://github.com/JapmeetBrar/pufferlab/pull/17#issuecomment-5384945636) passed local and exact-head GitHub gates but found that a persist/schedule crash could strand ownerless queued work, replay provenance conflated the production request with a second counterfactual probe, and the required fresh-checkout synthetic fallback had no serving/seed owner. The docs-only repair assigns startup claims/recovery to M3-D, freezes per-observation provenance and mismatch semantics for M3-A/M3-E, and assigns an idempotent SQLite-authoritative no-network 50-by-four seed to M3-B before dashboard/finalization work. |
| 2026-08-23 | M3-0 / [PR #17](https://github.com/JapmeetBrar/pufferlab/pull/17) | `implementing → review_requested` | The repair defines `queued -> running` claim-before-provider behavior, oldest-first startup reclamation, safe `queued -> failed` reconstruction failure, and stale-running interruption tests. It separates `stored_run`, `live_replay_primary`, `live_replay_counterfactual_probe`, and `client_computed` evidence with mismatch/failure semantics, and makes M3-B own an idempotent, content-addressed, explicitly synthetic 50-query/four-config/200-outcome SQLite seeder. `make check` passes with 354 backend tests plus one opt-in skip and six frontend tests/build; Ruff, format, mypy, OpenAPI drift, artifact audit `177/436/19`, and `git diff --check` pass. Exact-head re-review and reviewer-only merge are next. |
| 2026-08-23 | M3-0 / [PR #17](https://github.com/JapmeetBrar/pufferlab/pull/17) | `review_requested → changes_requested → implementing` | [Second independent review](https://github.com/JapmeetBrar/pufferlab/pull/17#issuecomment-5384971754) fully closed the queued-ownership and primary/counterfactual provenance blockers, but showed that exact synthetic canonical IDs could still be selected by create/recovery/replay and that seeded successes conflicted with observed `perf_counter` timing. The repair makes `synthetic_demo` read/export-only with direct redacted pre-factory rejection and zero-call tests, freezes disabled browser cost controls, uses `synthetic_unavailable` null/zero-sample timing, and requires all quality metrics/summaries to be recomputed from authored ranked IDs/qrels through the normal evaluator/aggregator. |
| 2026-08-23 | M3-0 / [PR #17](https://github.com/JapmeetBrar/pufferlab/pull/17) | `implementing → review_requested` | The second repair freezes direct pre-factory rejection for synthetic create, adversarial queued recovery, replay, and every provider-capable surface, with redacted typed errors and zero-call tests; the browser hides/disables those cost controls. Synthetic success timing is `synthetic_unavailable` with null total/stages, zero latency samples, and null percentiles, while quality is derived from authored ranks/qrels through normal production math. `make check` passes with 354 backend tests plus one opt-in skip and six frontend tests/build; Ruff, format, mypy, OpenAPI drift, artifact audit `177/438/19`, and `git diff --check` pass. Exact-head re-review and reviewer-only merge are next. |
| 2026-08-23 | M3-0 / [PR #17](https://github.com/JapmeetBrar/pufferlab/pull/17) | `review_requested → approved → merged → verified` | [Final independent re-review](https://github.com/JapmeetBrar/pufferlab/pull/17#issuecomment-5384987276) verified all queued recovery, replay provenance, synthetic read-only, unavailable timing, and normally derived quality decisions. The reviewer squash-merged PR #17 as `22abcc8b47fe03d200b82f299c4502c77ea1babf`; the [canonical protected-main record](https://github.com/JapmeetBrar/pufferlab/pull/17#issuecomment-5384993596) confirms Backend and Frontend passed. |
| 2026-08-23 | M3-A / `codex/m3-contracts` | `planned → assigned → implementing` | The evaluation contract worker starts from verified protected `main` `22abcc8` and owns only contract models, contract documentation/tests, OpenAPI, and generated TypeScript. Acceptance freezes the complete versioned HTTP data boundary, canonical 50-by-four request, direct redacted errors, read-only synthetic origin/unavailable timing, queued recovery transitions, per-observation replay provenance/certainty, and bounded allowlisted forensic evidence without importing provider, persistence, FastAPI composition, or handwritten browser domain types. |
| 2026-08-23 | M3-A / `codex/m3-contracts` | `implementing → review_requested` | The freeze adds versioned dataset/query-set/config catalogs; canonical create/list/detail/cancel/regression/query/export/replay envelopes; exact completed 50-by-four identities and six ordered summaries; explicit `live`/read-only `synthetic_demo` origin and `perf_counter`/`synthetic_unavailable` timing; honest unavailable original-stage evidence for M2; and strict typed forensic values with per-observation origin, timestamp, trace, and certainty. Old live dataset/success JSON decodes and re-encodes unchanged. The M2 live verifier adds only the authorized explicit null-latency rejection required by the widened success contract. `make check` passes with 385 backend tests plus one skip and six frontend tests/build; focused suites, static/format/schema drift, artifact audit `179/441/19`, exposure scan, and `git diff --check` pass. Dedicated review must inspect the complete branch and request fixes or merge through the reviewer-only loop. |
| 2026-08-23 | M3-A / [PR #18](https://github.com/JapmeetBrar/pufferlab/pull/18) | `review_requested → changes_requested → implementing` | [Independent review](https://github.com/JapmeetBrar/pufferlab/pull/18#issuecomment-5385121344) found that export/regression origin-timing claims were not fully bound to all durable payloads/summaries and that forensic rank/trace evidence was bounded but not always bound to the source response. The frontend consumer audit also found missing ordered config labels on run views, ambiguity between the existing Playground config route and the new persisted catalog, and replay eligibility named as if it were current namespace availability. The focused repair adds adversarial source-binding tests and contract/doc corrections before re-review. |
| 2026-08-23 | M3-A / [PR #18](https://github.com/JapmeetBrar/pufferlab/pull/18) | `implementing → review_requested` | The repair rejects synthetic exports with measured summaries, live exports with synthetic successes, synthetic regression latency, fabricated stored stage evidence, primary evidence without an actual result trace/time, impossible probe ranks/counts, and mismatched deep-link identities. Run views now include dataset identity plus four ordered config summaries, while `live_replay_policy_permitted` explicitly avoids claiming current namespace availability. Contract and plan docs preserve unscoped `/configs` for the Playground and assign persisted eval catalogs to `/datasets/{dataset_version_id}/configs`. `make check` passes with 391 backend tests plus one skip and six frontend tests/build; the 83-test focused suite, Ruff, format, mypy, OpenAPI/generated-TypeScript regeneration, artifact audit `179/455/19`, exposure scan, and `git diff --check` pass. Exact-head independent re-review is next. |
| 2026-08-23 | M3-A / [PR #18](https://github.com/JapmeetBrar/pufferlab/pull/18) | `review_requested → changes_requested → implementing` | [Second independent review](https://github.com/JapmeetBrar/pufferlab/pull/18#issuecomment-5385185727) verified the first repair and left one P1: `client_computed` values named a returned trace but could retain arbitrary time or unchecked rank-like inputs, while a stored `NOT_OBSERVABLE` observation could retain an invented timestamp. The narrow repair exact-binds client time/rank evidence to the matching primary/probe source and freezes stored unavailability with null trace/time before another full re-review. |
| 2026-08-23 | M3-A / [PR #18](https://github.com/JapmeetBrar/pufferlab/pull/18) | `implementing → review_requested` | The repair requires client-computed observation and evidence timestamps to equal the matching primary/probe observation, reuses positive probe count/rank validation for derived rank/membership/RRF inputs, authenticates primary-derived ranks against actual returned final/stage-membership ranks, and requires stored `NOT_OBSERVABLE` observation/evidence trace and time to remain null. Direct adversarial tests cover arbitrary primary/probe time, rank 100 against count 1, membership against count 0, fabricated primary rank, and stored trace/time. The 86-test focused suite and `make check` pass with 394 backend tests plus one skip and six frontend tests/build; schema regeneration is stable, artifact audit `179/463/19`, exposure scan, and `git diff --check` pass. Exact-head independent re-review is next. |
| 2026-08-23 | M3-A / [PR #18](https://github.com/JapmeetBrar/pufferlab/pull/18) | `review_requested → approved → merged → verified` | [Final independent re-review](https://github.com/JapmeetBrar/pufferlab/pull/18#issuecomment-5385231544) reproduced rejection of arbitrary client timestamps, impossible probe ranks, and stored fabricated traces while preserving valid source-bound counterparts. The reviewer squash-merged PR #18 as `5a66216f58d71a58787e90a4494fa771b98e0513`; the [canonical protected-main record](https://github.com/JapmeetBrar/pufferlab/pull/18#issuecomment-5385236264) confirms Backend and Frontend passed. |
| 2026-08-23 | M3-B / `codex/m3-eval-read-api` | `planned → assigned → implementing` | Root started from verified protected `main` `5a66216` and split disjoint ownership: eval_application implements bounded repository reads, strict provider-free run/regression/query/export views, catalog/eval HTTP routes, fake route tests, OpenAPI, and generated TypeScript; fixture_worker implements only deterministic authored seed inputs, the idempotent SQLite seeder/CLI, and no-network/recomputation/artifact tests. Both workers hand off to root; one combined PR receives independent whole-branch review and reviewer-only merge. |
| 2026-08-23 | M3-B / [PR #19](https://github.com/JapmeetBrar/pufferlab/pull/19) | `implementing → review_requested` | Fixture commit `3756b67` adds `pufferlab demo seed`: one content-addressed authored synthetic dataset, 50 queries/100 qrels, four canonical configs, and 200 normally evaluated successes with unavailable timing, idempotent golden export SHA-256 `b3bff5d09321ba535dd5bb09a95174a7d89e37a3a43720eabcdde8a33ac2b02e`, and poisoned network/runtime tests. Read/API commit `00c9b2c` adds bounded repository selectors; provider-free catalog/run/regression/query/export views; preserved unscoped Playground configs plus scoped eval catalogs; safe injected create/cancel/replay signatures; OpenAPI and generated TypeScript. The code-review integration pass found that foreign query IDs and valid-shaped false metrics could survive; repair `9f5e3da` binds every outcome to the exact 50 query IDs and recomputes three quality metrics from ranks plus graded qrels before pairing. `make check` passes with 416 backend tests plus one opt-in skip and six frontend tests/build; Ruff, format, mypy, OpenAPI drift, artifact audit `196/500/19`, exposure scan, and `git diff --check` pass. Bounded local-SQLite N+1-style run-list reads remain a documented P2; exact whole-branch review is next. |
| 2026-08-23 | M3-B / [PR #19](https://github.com/JapmeetBrar/pufferlab/pull/19) | `review_requested → changes_requested → implementing` | [Independent review](https://github.com/JapmeetBrar/pufferlab/pull/19#issuecomment-5385374111) reproduced a completed run whose row and payload were rewritten to queued while retaining 200 outcomes, summaries, and timestamps, plus safe errors whose formatted traceback retained the caught marker through `__context__`. The repair must derive complete query groups and lifecycle/summary validity from durable evidence before every run projection, and construct all public-safe translations outside active exception frames. |
| 2026-08-23 | M3-B / [PR #19](https://github.com/JapmeetBrar/pufferlab/pull/19) | `implementing → review_requested` | The repair now validates exact durable group progress and all six run-state timestamp/outcome/summary/error invariants before list, detail, or export projection; completed summaries are independently rebuilt with the production finalizer. Public-safe translations are raised only after the caught frame exits, with cause/context/repr/traceback and real HTTP attacks proving no internal marker survives. Fifteen focused application tests and `make check` pass with 424 backend tests plus one skip and six frontend tests/build; static/schema gates, artifact audit `196/502/19`, exact secret/browser scan, and `git diff --check` are clean. Dedicated exact-head re-review must close both P1s before reviewer-only merge. |
| 2026-08-23 | M3-B / [PR #19](https://github.com/JapmeetBrar/pufferlab/pull/19) | `review_requested → approved → merged → verified` | [Independent re-review](https://github.com/JapmeetBrar/pufferlab/pull/19#issuecomment-5385428827) reproduced every lifecycle, progress, summary, error-detachment, foreign-query, false-metric, no-key seed, and synthetic control proof. The reviewer squash-merged PR #19 as `87814c1caab34e263859ce7302ed14fb2627cd22`; the [canonical protected-main record](https://github.com/JapmeetBrar/pufferlab/pull/19#issuecomment-5385435196) confirms Backend and Frontend passed. |
| 2026-08-23 | M3-C / `codex/m3-run-dashboard` | `planned → assigned → implementing` | The frontend worker starts from verified protected `main` `87814c1`. A read-only generated-schema audit found no consumer blocker: all catalog/run/regression/query/control operations and direct errors are generated, the existing Playground route remains compatible, synthetic origin/unavailable timing/replay policy are explicit, and UUID-only URL state excludes licensed query text. Work is limited to the routed dashboard, generated-type client, lifecycle/polling/metric/regression UI, and deterministic accessibility/responsive tests. |
| 2026-08-23 | M3-C / [PR #20](https://github.com/JapmeetBrar/pufferlab/pull/20) | `implementing → review_requested` | The browser now preserves `/` and `/playground`, adds semantic `/runs` and `/runs/:runId` history, uses only generated response/request types, parses direct redacted errors, forwards abort signals, polls queued/running only, and distinguishes 0–50 query groups from 0–200 attempts. Metrics retain value/sample/null semantics and label client-wall latency as observed rather than benchmarked; partial summaries are explicitly non-final. Candidate/order/limit canonicalize into browser history, all excluded coverage is visible, and server-issued UUID-only Playground links contain no query text. Synthetic catalogs/runs are unmistakably read-only with unavailable timing and no enabled create/replay mutation. `pnpm install --frozen-lockfile`, generated-TypeScript zero-diff, lint, typecheck, 32 frontend tests, production build, and `make check` pass with 424 backend tests plus one opt-in skip. Dataset artifact audit `207/523/19`, source/build private-field scan, and `git diff --check` pass. No credential was available for an exact-key scan, and the required browser-control runtime reported zero available browser instances after its documented troubleshooting flow, so interactive 320/390 px visual inspection remains an explicit reviewer/finalization path; responsive breakpoints and table containment are implemented and deterministic route/accessibility behavior is covered. Independent exact-head review must inspect, test, request fixes if needed, and reviewer-only merge. |
| 2026-08-23 | M3-C / [PR #20](https://github.com/JapmeetBrar/pufferlab/pull/20) | `review_requested → approved → merged → verified` | Root independently served the exact head against a fresh provider-free synthetic SQLite seed and verified 320/390 px containment, heading focus, real-link history, regression URL back/forward/refresh, synthetic no-cost behavior, null timing, coverage, Playground preservation, and 404 handling. [Dedicated review](https://github.com/JapmeetBrar/pufferlab/pull/20#issuecomment-5385563568) found no blocker and reviewer-only squash-merged PR #20 as `ed549df87307e877cbe5da8bb1a463af4ebabc87`; the [canonical protected-main record](https://github.com/JapmeetBrar/pufferlab/pull/20#issuecomment-5385569932) confirms Backend and Frontend passed. |
| 2026-08-23 | M3-D / `codex/m3-eval-control-runtime` | `planned → assigned → implementing` | Root started from verified protected `main` `ed549df`. The read-only preflight identified the exact risks to close: warmups before a running claim, reordered canonical configs accepted at creation, fatal manager failures without durable safe errors, missing queued-to-failed/active selectors, no default migrate/recovery lifecycle, and provider construction before pure exact-binding validation. Implementation is constrained to the API runtime, application/job/repository/retrieval seams, focused tests, one-worker documentation, and this ledger; replay and frontend work wait for M3-E. |
| 2026-08-23 | M3-D / `codex/m3-eval-control-runtime` | `implementing → review_requested` | Persistence commit `749612d` adds bounded oldest-first active selection, atomic queued claims, queued-to-failed recovery, and mandatory contract-valid safe failure details. Application/job commit `1b1956f` makes CLI and API execution claim before warmup/executor work, freezes exact mode order, preserves cooperative cancellation, and safely detaches every fatal path. Root adds the guarded default API owner, pure pre-runtime config identity derivation, duplicate/global admission, request-independent durable 202 drivers, restart reclamation, idempotent controls, and ordered shutdown. Adversarial tests prove post-commit recovery, stale interruption, oldest-first capacity, invalid and synthetic pre-factory failures, zero credential/catalog/runtime/provider/embedder/reranker construction, safe post-claim failure, second-worker rejection, and eval→Playground→SQLite close order. `make check` passes: Ruff, format, mypy, 448 backend tests plus one opt-in skip, OpenAPI zero-diff, frontend lint/typecheck, 32 tests, and production build. Independent exact-head review must inspect and re-run risk-relevant gates before reviewer-only merge. |
| 2026-08-23 | M3-D / [PR #21](https://github.com/JapmeetBrar/pufferlab/pull/21) | `review_requested → changes_requested → implementing` | [Independent review](https://github.com/JapmeetBrar/pufferlab/pull/21#issuecomment-5385675240) confirmed the one-worker, durable ordering, claim-before-factory, synthetic zero-call, request-independent scheduling, cancellation, and shutdown design, but reproduced one P1 safety defect: `raise ... from None` inside caught frames left private startup/create/cancel/file-lock exceptions reachable through `__context__`. The review also required this PR link and repair cycle in the ledger. PR #21 remains unmerged until exact-head re-review. |
| 2026-08-23 | M3-D / [PR #21](https://github.com/JapmeetBrar/pufferlab/pull/21) | `implementing → review_requested` | The repair clones or builds each `EvaluationViewError` inside its catch and raises only after the caught frame exits; low-level open/flock helpers return bounded status instead of raising public errors from OS exception handlers. Direct attacks cover nested view errors, missing/validation/runtime create paths, post-validation persistence failure, missing/storage cancel paths, startup context, and open/blocked/failed file locks; every assertion requires null cause/context and no marker in string, repr, or the complete rendered traceback. HTTP create/cancel attacks also return redacted 503 bodies. Eighty risk-focused tests pass. Full exact-head `make check` passes with 451 backend tests plus one opt-in skip, OpenAPI zero-diff, 32 frontend tests, and production build; reviewer re-review and merge remain required. |
| 2026-08-23 | M3-D / [PR #21](https://github.com/JapmeetBrar/pufferlab/pull/21) | `review_requested → approved → merged → verified` | [Independent re-review](https://github.com/JapmeetBrar/pufferlab/pull/21#issuecomment-5385708016) reproduced eleven startup/create/cancel/file-lock marker attacks with null cause/context and no marker in full rendered tracebacks, reran 80 focused tests and the complete gate, and approved exact head `c438f89`. Reviewer-only squash merge produced `ef55645bd4edcc2fc739f0219bfb23b4d51a8d3c`; the [canonical record](https://github.com/JapmeetBrar/pufferlab/pull/21#issuecomment-5385713819) confirms protected-main Backend and Frontend passed. |
| 2026-08-23 | M3-E / `codex/m3-query-forensics` | `planned → assigned → implementing` | Root branched from verified protected `main` `ef55645`. The backend worker owns only replay binding/composition, distinct primary/probe observations, exact graded-qrel forensic rules, and provider-zero-call attacks. The frontend worker owns only `web/**`: generated-type consumers, explicit replay UI, UUID-only run/query/config/document URL restoration, and accessible drawer behavior. The shared contract is already frozen; changes that require contract drift stop for root review. Root integrates, runs full gates and browser QA, opens one PR, and the dedicated reviewer alone may approve and merge. |
| 2026-08-23 | M3-E / `codex/m3-query-forensics` | `implementing → contract_repair_assigned` | Independent backend and frontend preflights both found that the frozen response cannot safely bind an observation to the drawer's exact config/document, and cannot represent a failed counterfactual attempt with its own trace/time. Root authorized a minimal additive repair before provider or UI implementation: typed observation target IDs plus a bounded failed-probe source, with cross-config/document/trace/time substitution tests. The repair may not expand persistence, accept browser query text/namespace, weaken source separation, or invent original stored evidence; OpenAPI and generated TypeScript must move together. |
| 2026-08-23 | M3-E / `codex/m3-query-forensics` | `contract_repair_assigned → implementing` | Contract commit `315c79b` adds exact config/document targeting and bounded failed-probe sources, authenticates ranks/scores/presence/counts/RRF inputs against the exact returned source, rejects cross-source substitutions, and regenerates OpenAPI/TypeScript. Backend commit `1dd9cb3` adds exact-bound request-scoped replay, distinct primary traces, separate hybrid probe traces, exact graded qrels, pure noncausal forensic rules, safe closure/redaction, and synthetic/tampered pre-factory rejection. Frontend commit `3cb2711` adds provider-free query routes, UUID-only canonical history, explicit replay, source-separated typed evidence, and the focus-trapped URL-driven drawer. |
| 2026-08-23 | M3-E / [PR #22](https://github.com/JapmeetBrar/pufferlab/pull/22) | `implementing → review_requested` | Root `make check` passes with Ruff, format, strict mypy, 471 backend tests plus one opt-in live skip, OpenAPI zero-diff, frontend lint/typecheck, 48 tests, and production build. Artifact audit passes at `219/575/19`; a synthetic-marker exact secret-boundary scan passes across 219 tracked files, 575 history blobs, and 25 browser files. Root browser QA against a fresh provider-free 50-by-four seed verifies desktop/720/390/320 containment, internal table scrolling, both route forms, GET-only refresh, synthetic zero-POST policy, fixed stored `NOT_OBSERVABLE`, drawer focus trap/Escape/return, Back/Forward restoration, clean console, and UUID-only URLs; only health/query-detail GETs appeared in server logs. No credentialed live replay was run. The temporary QA database was moved to Trash and no data/evidence artifact entered Git. |
| 2026-08-23 | M3-E / [PR #22](https://github.com/JapmeetBrar/pufferlab/pull/22) | `review_requested → changes_requested → implementing` | [Independent review](https://github.com/JapmeetBrar/pufferlab/pull/22#issuecomment-5385932786) reproduced two P1 defects while accepting every other gate: a valid-shaped direct SQLite qrel substitution reached credential/runtime construction because replay authenticated only one stored query, and a second cancellation during blocking `backend.close()` abandoned request-scoped cleanup. The focused repair is limited to immutable full-suite authentication before provider-capable boundaries, cancellation-resilient cleanup draining, adversarial tests for both attacks, and this ledger; PR #22 remains unmerged pending exact-head re-review. |
| 2026-08-23 | M3-E / [PR #22](https://github.com/JapmeetBrar/pufferlab/pull/22) | `implementing → implementing` (focused repair handoff) | The checked ID-only curation manifest now anchors the exact licensed query/qrel content hash without storing source text. Replay loads all 50 persisted queries, validates source-lock, curation order/IDs/tags, canonical query/qrel identities, full content hash, and query-set UUID before the dataset manifest, credential, catalog, runtime, or provider-capable factories. Independent SQLite attacks substitute the document UUID, grade, and both together and reach no live boundary. Request-scoped close is an owned task drained through repeated cancellation while the original cancellation result is preserved; close failures remain detached. Fifty-one focused dataset/runtime tests, Ruff, format, strict mypy, diff checks, and a read-only validation of the existing ignored live database against the checked anchor pass. Root integration review and the complete gate remain before a new exact-head review request. |
| 2026-08-23 | M3-E / [PR #22](https://github.com/JapmeetBrar/pufferlab/pull/22) | `implementing → review_requested` | Root inspected the full repair diff and independently reran the exact document-only, grade-only, combined-substitution, full-suite mutation, double-cancel, and close-failure paths. `make check` passes with Ruff, format, strict mypy, 476 backend tests plus one opt-in live skip, OpenAPI zero-diff, frontend lint/typecheck, 48 tests, and production build; generated TypeScript is stable. Artifact audit passes at `219/588/19`, and the exact configured-key boundary passes across 219 tracked files, 588 history blobs / 9,524,363 bytes, and 25 browser files. The checked query-set digest reproduces the canonical live query-set UUID, and the existing ignored live database authenticates read-only. Re-review must use the new immutable pushed head and only the dedicated reviewer may merge. |
| 2026-08-23 | M3-E / [PR #22](https://github.com/JapmeetBrar/pufferlab/pull/22) | `review_requested → approved → merged → verified` | [Independent exact-head re-review](https://github.com/JapmeetBrar/pufferlab/pull/22#issuecomment-5386008445) separately rejected document-only, grade-only, and combined SQLite qrel substitutions before every live factory, drained three cancellations through blocking close while preserving the first cancellation, retained detached close errors, and authenticated both ignored canonical live suites without changing database bytes, mtimes, or sidecars. The reviewer squash-merged PR #22 as `43ee692c9886085a94ca0f9b66a720e53a609067`; the [canonical record](https://github.com/JapmeetBrar/pufferlab/pull/22#issuecomment-5386015555) confirms protected-main Backend and Frontend passed. |
| 2026-08-23 | M3-F / `codex/m3-finalization` | `planned → assigned → implementing` | Root branched from verified protected `main` `43ee692`. Finalization owns only README/demo/observability/contract documentation, bounded browser-smoke support if needed, this ledger, and sanitized verification evidence. It must prove an empty-directory synthetic 50-by-four dashboard, an explicit credentialed live replay against an exact stored run/query/config pair, refresh/back navigation at desktop and mobile widths, no implicit provider work, exact secret/artifact boundaries, and the complete local gate before one independent final review and reviewer-only merge. |
| 2026-08-23 | M3-F / `codex/m3-finalization` | `implementing → review_requested` | A new ignored directory seeded the same content-addressed 50-query/four-config/200-outcome synthetic run twice with identical database SHA-256 and mtime; serving it added only the process guard, and all QA traffic was health/run/regression/query-detail GETs. Browser QA at 1280/720/390/320 verifies page containment, internally scrollable tables, UUID-only candidate/order/query/document state, reload and Back/Forward restoration, drawer focus trap/Escape/opener return, no replay control, and a clean console. One explicit credentialed replay against stored live run `8eded793-6337-443a-98f3-623e1ad465a5` returned HTTP 200 with two distinct primary traces, one bounded observation, no optional probes, and `original_stage_evidence_available=false`; the disposable and original databases retained their byte hashes with no sidecars, and the original database mtime was unchanged. An initial redacted 503 identified the locally absent optional model runtime; `uv sync --locked --extra live-search` repaired only the ignored environment before the successful retry. Both disposable directories were moved recoverably to Trash, no namespace was created or deleted, and unrelated user servers were untouched. `make check` passes with 476 backend tests plus one opt-in live skip, OpenAPI zero-diff, frontend lint/typecheck, 48 tests, and production build; frozen installs, generated TypeScript, docs links, diff, full-history artifact, and exact configured-key/browser scans pass. The exact pushed head, PR checks, and dedicated reviewer decision remain. |
| 2026-08-23 | M3-F / [PR #23](https://github.com/JapmeetBrar/pufferlab/pull/23) | `review_requested → changes_requested → implementing` | [Independent exact-head review](https://github.com/JapmeetBrar/pufferlab/pull/23#issuecomment-5386113566) passed deterministic double seeding, isolated one-worker enforcement, responsive browser/history/focus QA, `make check`, generated-code drift, artifact/privacy scans, and both required GitHub checks. It stopped before the optional live replay and correctly blocked merge because the older M1-E link targeted nonexistent `implementation-plan.md#task-t7`; root repaired it to the heading's GitHub anchor `#t7--cli-workflows` and began the bounded docs-only revalidation cycle. |
| 2026-08-23 | M3-F / [PR #23](https://github.com/JapmeetBrar/pufferlab/pull/23) | `implementing → review_requested` | The docs-only repair validates all 17 tracked Markdown files, 53 local link targets, and 21 Markdown fragments, including the corrected T7 heading anchor. `make check` again passes 476 backend tests plus one opt-in live skip, OpenAPI drift, lint, typecheck, 48 frontend tests, and production build; generated TypeScript and `git diff --check` are clean. Full-history artifact and exact configured-key/browser scans pass; their immutable-head counts belong in the PR review handoff so this commit cannot stale its own evidence. The already-successful bounded live replay was deliberately not repeated for this documentation repair. |
| 2026-08-23 | M3-F / [PR #23](https://github.com/JapmeetBrar/pufferlab/pull/23) | `review_requested → approved → merged → verified` | [Independent exact-head re-review](https://github.com/JapmeetBrar/pufferlab/pull/23#issuecomment-5386152705) verified the repaired GitHub anchor, all 17 Markdown files/53 local targets/21 fragments, generated drift, artifact and exact-key boundaries, and successful PR checks. The reviewer squash-merged as `a7c2800c49b9e60268266fb3dfdcc25a45a18e1e`; the [canonical record](https://github.com/JapmeetBrar/pufferlab/pull/23#issuecomment-5386159858) confirms fresh protected-main [Backend](https://github.com/JapmeetBrar/pufferlab/actions/runs/32641367627/job/97198850881) and [Frontend](https://github.com/JapmeetBrar/pufferlab/actions/runs/32641367627/job/97198850773) passed. |
| 2026-08-23 | M4-0 / `codex/m4-plan` | `planned → assigned → implementing` | Root started from canonical M3 merge `a7c2800` and assigned three read-only audits across current product gaps, code seams, and official provider capabilities. The observed missing-namespace failure and absence of repeatable browser CI justify local capability guidance; the original P1 CI gate is the smallest provider-free deployment feature. Generic import, arbitrary branching, warm hints, and ANN recall are explicitly deferred rather than mixed with cleanup authority. |
| 2026-08-23 | M4-0 / `codex/m4-plan` | `implementing → review_requested` | Three independent audit loops now approve the repaired scope. Their blockers froze two separate clean-checkout success tracks, reachable-only contract generation, a read-only doctor/gate composition, loopback one-worker serving, receipt-bound credential/region authority, fixed per-user no-follow locking and atomic lifecycle transitions through terminal cleanup, honest local evidence trust, exact paired gate math, and provider-free browser CI. `make check` passes Ruff, format, strict mypy, 476 backend tests plus one opt-in live skip, OpenAPI drift, frontend lint/typecheck, 48 tests, and production build. All 18 Markdown files, 56 local links, and 22 fragments validate; exact-head artifact/privacy checks and dedicated reviewer-only merge remain. |
| 2026-08-23 | M4-0 / [PR #24](https://github.com/JapmeetBrar/pufferlab/pull/24) | `review_requested → changes_requested → implementing` | [Independent exact-head review](https://github.com/JapmeetBrar/pufferlab/pull/24#issuecomment-5387151629) passed `make check`, 18/56/22 Markdown validation, artifact and exact-key scans, generated drift, and required Backend/Frontend CI, but found two plan blockers. Receipt corruption had no frozen code in the capability allowlist, and `doctor --live` promised one potentially billable request while pinned turbopuffer 2.9 defaults to four retries. The repair adds `owned_tiny_receipt_invalid`, requires `max_retries=0` with no application retry, and assigns one-attempt tests across retryable status, transport, and timeout failure. |
| 2026-08-23 | M4-0 / [PR #24](https://github.com/JapmeetBrar/pufferlab/pull/24) | `implementing → review_requested` | The repair makes invalid fixed receipt bytes an explicit fail-closed `owned_tiny_receipt_invalid` state and requires the pinned provider client to use `max_retries=0` with no application retry. M4-B acceptance now proves one outbound attempt for retryable status, transport, and timeout failures. `make check` again passes 476 backend tests plus one skip, generated drift, frontend lint/typecheck, 48 tests, and build; 18 Markdown files, 56 links, 22 fragments, and `git diff --check` pass. Exact-head re-review is required. |
| 2026-08-23 | M4-0 / [PR #24](https://github.com/JapmeetBrar/pufferlab/pull/24) | `review_requested → approved → merged → verified` | [Independent exact-head re-review](https://github.com/JapmeetBrar/pufferlab/pull/24#issuecomment-5387168021) verified both repairs, full local/privacy gates, and required PR checks, then reviewer-only squash-merged as `0e244ce6551d7938f136e842110d964bab04c844`. The [canonical record](https://github.com/JapmeetBrar/pufferlab/pull/24#issuecomment-5387174910) confirms protected-main [Backend](https://github.com/JapmeetBrar/pufferlab/actions/runs/32652993054/job/97227356055) and [Frontend](https://github.com/JapmeetBrar/pufferlab/actions/runs/32652993054/job/97227355956) passed. |
| 2026-08-23 | M4-A / `codex/m4-contracts` | `planned → assigned → implementing` | Root branched from verified M4-0 merge `0e244ce` and assigned a contract-only worker. Acceptance freezes allowlisted capability states/requirements/actions, direct `configuration_required`, finite gate inputs/reports and deterministic check ordering, safe serialization, and secret/path/namespace field absence. Only the reachable error-enum change may move OpenAPI/generated TypeScript; the capabilities path remains owned by M4-B and gate models remain CLI-only. |
| 2026-08-23 | M4-A / `codex/m4-contracts` | `implementing → review_requested` | The contract head freezes seven unique ordered capability requirements with one-to-one safe actions, configured/action-required invariants, three quality metrics, strict finite policy domains, and typed checks ordered `candidate_error_rate`, coverage, aggregate delta, then per-query drop. Reports bind distinct configs, exclude every candidate failure from the paired population, enforce inclusive thresholds, derive a two-state verdict, and retain at most ten unique delta/UUID-ordered violations. The 87-test contract suite, strict mypy, deterministic generators, and full `make check` pass: 500 backend tests plus one opt-in skip, OpenAPI drift, frontend lint/typecheck, 48 tests, and production build. Generated diffs contain only the reachable `ApiErrorCode`; tests prove the capabilities path and every Capability/Gate component remain absent. No provider call, credential read, route, CLI, persistence, runtime, or UI behavior was added. Dedicated exact-head review and reviewer-only merge are next; M4-B owns runtime/web manual-code handling for `configuration_required`. |
| 2026-08-23 | M4-A / [PR #25](https://github.com/JapmeetBrar/pufferlab/pull/25) | `review_requested → changes_requested → implementing` | [Independent review](https://github.com/JapmeetBrar/pufferlab/pull/25#issuecomment-5387241492) reproduced a false pass with one failed candidate, submitted rate `0.0199999999995`, threshold `0.01999999999975`, and `passed=true`: the `1e-12` tolerance accepted a rate inconsistent with the exact `1/50`, then trusted that approximation for the verdict. Review requires exact equality to the count-derived rate, a verdict derived from that exact rate, and the symmetric false-failure regression before re-review. |
| 2026-08-23 | M4-A / [PR #25](https://github.com/JapmeetBrar/pufferlab/pull/25) | `implementing → review_requested` | The repair removes tolerance: `observed_error_rate` must equal the exact integer failed-count divided by 50, and pass/fail is computed from that exact count-derived rate. Tests reject the reviewer's false pass and the symmetric false failure while preserving strict bool/string and NaN/infinity rejection. The 89-test contract suite and full `make check` pass with Ruff, format, strict mypy, 502 backend tests plus one opt-in skip, OpenAPI drift, frontend lint/typecheck, 48 tests, and production build. No provider call or credential read occurred; dedicated exact-head re-review is required. |
| 2026-08-23 | M4-A / [PR #25](https://github.com/JapmeetBrar/pufferlab/pull/25) | `review_requested → approved → merged → verified` | [Independent exact-head re-review](https://github.com/JapmeetBrar/pufferlab/pull/25#issuecomment-5387261362) independently rejected both approximate attacks, round-tripped all 51 exact failed-count rates, passed 89 contract tests and generated/artifact gates, and reviewer-only squash-merged as `8abdb3294d443d83cd9759ec38b8abdcb38b9ec8`. The [canonical record](https://github.com/JapmeetBrar/pufferlab/pull/25#issuecomment-5387268448) confirms protected-main [Backend](https://github.com/JapmeetBrar/pufferlab/actions/runs/32654084297/job/97230038988) and [Frontend](https://github.com/JapmeetBrar/pufferlab/actions/runs/32654084297/job/97230038954) passed. |
| 2026-08-23 | M4-B / `codex/m4-readiness` | `planned → assigned → implementing` | Root started from verified M4-A merge `8abdb32` and split serial ownership across three implementation agents. Provider preflight showed pinned SDK 2.9 defaults to four retries, follows redirects, reads base-URL/custom-header environment overrides, and retains the unwrapped key until close. The dedicated probe must pin official HTTPS host, disable retries/redirects/proxy inheritance, enforce a finite timeout and one exact Authorization header, issue only metadata GET, and drain close through repeated cancellation. Readiness core then owns provider-free capability/search preflight and generated/manual web contracts; CLI integration owns existing-file read-only doctor selection and loopback one-worker serve. M4-C later fills the injected authenticated owned-tiny resolver. |
| 2026-08-23 | M4-B / `codex/m4-readiness` | `implementing → implementing` (provider probe handoff) | The dedicated public metadata probe owns a strict turbopuffer 2.9 async client with one validated official-region target, `max_retries=0`, a ten-second timeout, no proxy inheritance or redirects, and a pre-transport hook that rejects non-metadata method/path/body shapes and replaces all SDK/environment headers with one allowlisted Authorization value. It returns only four value-free states, consumes malformed/provider/close failures without their exception graphs, and drains exactly one close through repeated cancellation. Twenty-nine focused real-SDK/HTTPX transport tests cover encoded paths, hostile region/base/header/proxy environments, unique auth, 404/429/500/timeout/connect/redirect one-attempt behavior, poisoned writes/queries/deletes, malformed bodies, and request/close/double-cancel paths. Provider tests, strict mypy, Ruff/format, full `make check` (531 passed, one opt-in skip; 48 frontend tests/build), OpenAPI drift, artifact audit `226/624/19`, and diff checks pass. No real credential was read and no network/provider call occurred; readiness-core integration remains next. |
| 2026-08-23 | M4-B / `codex/m4-readiness` | `implementing → implementing` (provider probe security repair) | Integration preflight found that freshly raised safe configuration/cancellation exceptions still retained credential, namespace, and transport objects in the public coroutine traceback frame. The repair moves all provider work and close draining behind a non-raising safe outcome, scrubs every caller-supplied reference before separate value-free helpers raise, makes the raw-key request hook and request error private, and rejects blank credentials plus blank or overlong UTF-8 namespaces before client construction. Thirty-five dedicated probe tests now inspect library traceback locals during hostile configuration, request cancellation, and repeated close cancellation; they prove zero transport for invalid local input and exclude HTTPX request/client/transport objects and submitted values from every retained library frame. Ruff, format, strict mypy, full `make check` (537 passed, one opt-in skip; 48 frontend tests/build), OpenAPI drift, artifact audit `226/628/19`, and diff checks pass. No real credential was read and no network/provider call occurred; whole-branch integration review remains required. |
| 2026-08-23 | M4-B / `codex/m4-readiness` | `implementing → implementing` (readiness core assigned) | The readiness-core worker owns only the pure local capability inspector and M4-C receipt-resolver seam, side-effect-free capabilities HTTP route, pre-factory typed search configuration failure, reachable OpenAPI/generated TypeScript, and the manual web error-code guard. It must prove deterministic requirement ordering, blank-as-missing behavior, no provider/model/database work from inspection, invalid-filter-first search behavior, and absence of every CLI-only Gate schema; doctor/serve and all CLI files remain for the serial CLI worker. |
| 2026-08-23 | M4-B / `codex/m4-readiness` | `implementing → implementing` (readiness core handoff) | The pure inspector shares one frozen-order local preflight with runtime search, uses package-spec discovery without importing `sentence_transformers`, and exposes an injected receipt resolver whose M4-B default fails closed until M4-C. `GET /api/v1/capabilities` is now reachable and generated while every CLI-only Gate schema stays absent. Missing/blank key, namespace, region, or runtime returns direct `configuration_required` HTTP 503 after filter validation and before provider/embedder/reranker factories; valid runtime failures retain their prior semantics. Architecture preflight caught an initial plaintext-read violation: the repair normalizes blank raw config at Settings construction, makes inspector/search preflight presence-only, and uses hostile `SecretStr` tests to prove no default unwrap. `make check` passes with 556 backend tests plus one opt-in skip, generated OpenAPI drift, frontend lint/typecheck, 49 tests, and production build; generated TypeScript is stable, artifact audit passes `231/632/19`, synthetic secret-boundary scan passes `226/632/25`, and diff checks pass. No real credential, provider call, database migration/write, model import/construction, or CLI/UI behavior occurred; doctor/serve integration and whole-branch review remain next. |
| 2026-08-23 | M4-B / `codex/m4-readiness` | `implementing → implementing` (doctor assigned) | The doctor worker owns only the dedicated existing-file immutable SQLite composition, deterministic demo/evaluation/live-tiny selection, safe finite doctor reports and exits, explicit one-shot metadata bridge, early parser/dispatch, and focused adversarial tests. The default path must remain provider-free and byte/mtime/sidecar stable; `live-tiny` uses the injected fail-closed M4-C resolver; serve remains a separate serial worker and no credentialed call is authorized. |
| 2026-08-23 | M4-B / `codex/m4-readiness` | `implementing → implementing` (doctor handoff) | The installed early-dispatch doctor now opens only one strict existing regular SQLite file through an immutable read-only URI, rejects hot sidecars/corruption/FK or revision drift, reuses repository plus provider-free run-view validation, and rechecks device/inode/size/mtime and sidecars after disposal. Demo inspection exact-matches deterministic identities/evidence; evaluation selection is explicit or sole-cardinality, READY, canonical 50-by-four, and requires a structurally validated completed run. Explicit live evaluation resolves only a persisted LIVE namespace and mutually agreeing stored run region, checks the current region, unwraps the key only in the scrubbed one-call bridge, and maps cancellation to fixed exit `130`; live-tiny retains the injected M4-C fail-closed resolver. Thirty-six focused adversarial tests cover absent/symlink/nonregular/metachar/corrupt/truncated/wrong-revision/FK/sidecar/identity-swap catalogs, byte/hash/mtime stability, parser poisoning, exact demo and catalog tampering, zero/multiple/explicit dataset selection, synthetic-live rejection, stored-versus-environment target resolution, region conflicts/mismatch, oversized namespace, every probe state, single-call/order/exit precedence, redaction, and exception-frame scrubbing. Full `make check` passes with 592 backend tests plus one opt-in skip and 49 frontend tests/build; OpenAPI drift, artifact audit `235/650/19`, synthetic secret scan `235/650/25`, and diff checks pass. No real credential, network, provider/model construction, migration, recovery, write, or directory/sidecar creation occurred during doctor execution. Serve remains the next serial subtask before whole-branch review. |
| 2026-08-23 | M4-B / `codex/m4-readiness` | `implementing → implementing` (serve assigned) | The serve worker owns only strict early parser/dispatch, immutable loopback-only one-worker Uvicorn composition, and a whole-synchronous-process shutdown bound with fixed safe output. Tests use allocated loopback ports and injected private seams only; they must prove invalid input constructs no settings/server, first and repeated signal behavior, watchdog hard-exit fallback, isolated installed-command startup/health/shutdown, socket release, and safe startup/bind failure. Existing servers, credentials, provider paths, doctor behavior, migrations, and runtime recovery remain untouched. |
| 2026-08-23 | M4-B / `codex/m4-readiness` | `implementing → implementing` (serve handoff) | The installed early-dispatch serve command accepts only exact loopback hosts and ASCII integer ports, canonicalizes `localhost`, constructs no Settings or normal CLI application before dispatch, and fixes the direct app, one worker, no reload/proxy/access log, critical logging, and five-second Uvicorn task-drain timeout. An outer signal scope covers asyncio runner construction; first SIGINT/SIGTERM starts exactly one daemon ten-second watchdog, repeated signal forces Uvicorn draining, inner signal capture never replays either signal, and completion is recorded only after the full synchronous runner returns. A deliberately cancellation-ignoring lifespan subprocess proves the outer hard exit for both signals; documentation records that this recovery fallback skips Python cleanup and relies on OS socket/lock release plus next-start SQLite migration/recovery. Thirty-three focused tests cover parser poisoning, exact config, safe BaseException/SystemExit/startup/bind failure, watchdog/thread failure, installed health and both graceful signals, guard/socket release, and both hung-lifespan signals using only allocated ports. Full `make check` passes with 625 backend tests plus one opt-in skip, OpenAPI drift, frontend lint/typecheck, 49 tests, and production build; post-commit artifact audit `238/663/19`, synthetic secret-boundary scan `238/663/25`, diff, Ruff/format, and strict mypy pass. No real credential, provider/network call, user server, or port 8000/5173 was accessed; whole-branch integration review is next. |
| 2026-08-23 | M4-B / `codex/m4-readiness` | `implementing → review_requested` | Root inspected all five implementation commits and independently reran the provider, readiness, doctor, read-only catalog, serve, contract, frontend, and generated-schema risk paths. `make check` passes with Ruff, formatting, strict mypy, 625 backend tests plus one opt-in live skip, OpenAPI drift, frontend lint/typecheck, 49 tests, and production build. The full-history artifact audit passes `238/663/19`; the exact configured-key scan passes across 238 tracked files, 663 Git blobs / 12,489,121 bytes, and 25 production browser files; generated OpenAPI/TypeScript and `git diff --check` are clean. No provider call, real namespace operation, user-server access, or credential output occurred. The exact pushed head now requires dedicated whole-branch review and reviewer-only merge. |
| 2026-08-23 | M4-B / [PR #26](https://github.com/JapmeetBrar/pufferlab/pull/26) | `review_requested → changes_requested → implementing` | [Dedicated review](https://github.com/JapmeetBrar/pufferlab/pull/26#issuecomment-5387708154) found that doctor validation and repository reads could use different SQLite file handles after pathname replacement, raw-key cancellation/SystemExit paths were not uniformly scrubbed, the CI traceback helper admitted caller frames because the checkout directory is named `pufferlab`, and serve had a separate shutdown race. The doctor worker owns the first, second, and fourth review findings as numbered in that review; the serve worker owns the remaining isolated repair before exact-head re-review. |
| 2026-08-23 | M4-B / [PR #26](https://github.com/JapmeetBrar/pufferlab/pull/26) | `implementing → implementing` (doctor review-repair handoff) | Doctor now opens and configures one SQLite handle before a one-shot `StaticPool` creator can claim it; validation and all repository sessions share that exact immutable handle, while any invalidation/reconnect attempt fails instead of reopening a replaced pathname. Deterministic attacks swap a second valid catalog after validation, prove reads retain the original dataset, restore both exact files, and prove invalidation cannot select the replacement. The raw-key bridge catches and detaches both cancellation types after unwrap, maps unwrap/probe `SystemExit(77)` to finite internal exit `1`, and scrubs settings, secret, key, target, and probe references before every return. The CI traceback filter now uses the exact production module filename and a caller-marker regression. The 45 focused tests pass three consecutive runs; strict mypy, Ruff/format, full `make check` (634 backend tests plus one opt-in skip; 49 frontend tests/build), generated drift, artifact audit `238/664/19`, synthetic secret-boundary scan `238/664/25`, and diff checks pass. No real key, provider/network request, migration, write, sidecar, or user server was used. The separate serve repair remains before root can request whole-head re-review. |
| 2026-08-23 | M4-B / [PR #26](https://github.com/JapmeetBrar/pufferlab/pull/26) | `implementing → implementing` (serve review repair assigned) | The serve worker owns the remaining isolated reviewer blocker: a supported signal flag must not convert a later runtime `BaseException` into graceful exit `0`. The finite execution result will require both signal observation and a clean synchronous return, with hostile `RuntimeError`, `SystemExit`, and property-access regressions proving fixed exit `1` and redacted output. The watchdog's real hard `os._exit(0)` path, installed graceful signal behavior, hung-lifespan bound, immutable Uvicorn settings, doctor/provider/readiness code, and user servers remain unchanged. The adjacent metadata-client plan wording will also state explicitly that `trust_env=False` disables environment proxy inheritance. |
| 2026-08-23 | M4-B / [PR #26](https://github.com/JapmeetBrar/pufferlab/pull/26) | `implementing → implementing` (serve review repair handoff) | Serve success now requires both a supported shutdown signal and a clean return from the entire synchronous runner. A signal followed by marker-bearing `RuntimeError`, `SystemExit`, or `KeyboardInterrupt` returns fixed exit `1` with only the safe failure line; a signal-status property failure does the same. Normal installed SIGINT/SIGTERM still returns `0`, the cancellation-ignoring lifespan subprocess still reaches the private shortened hard `os._exit(0)` path, and a clean return without a signal remains failure. The metadata plan now unambiguously says environment proxy inheritance is disabled with `trust_env=False`, alongside disabled redirects. The 37-test serve suite passes three consecutive runs, all 35 metadata-probe tests pass, and full `make check` passes with 638 backend tests plus one opt-in skip, OpenAPI drift, frontend lint/typecheck, 49 tests, and production build. Post-commit artifact audit `238/673/19`, synthetic secret-boundary scan `238/673/25`, generated drift, and diff checks pass. No real credential, provider request, user server, or port 8000/5173 was accessed; root whole-head validation and exact-head re-review remain. |
| 2026-08-23 | M4-B / [PR #26](https://github.com/JapmeetBrar/pufferlab/pull/26) | `implementing → review_requested` (repaired whole head) | Root inspected both review repairs and independently reran 117 provider/readiness/read-only/doctor/serve adversarial tests plus strict typing on the repaired modules. `make check` passes with Ruff, formatting, strict mypy, 638 backend tests plus one opt-in live skip, OpenAPI drift, frontend lint/typecheck, 49 tests, and production build. The artifact audit passes `238/673/19`; the exact configured-key scan passes across 238 tracked files, 673 Git blobs / 12,981,133 bytes, and 25 production browser files; generated OpenAPI/TypeScript and diff checks are clean. No provider request, namespace operation, credential output, user-server access, or use of ports 8000/5173 occurred. The immutable repaired head now requires dedicated exact-head re-review; only that reviewer may merge. |
| 2026-08-23 | M4-B / [PR #26](https://github.com/JapmeetBrar/pufferlab/pull/26) | `review_requested → changes_requested → implementing` (second re-review) | [Second dedicated re-review](https://github.com/JapmeetBrar/pufferlab/pull/26#issuecomment-5387794314) transiently substituted valid catalog B only during `sqlite3.connect` after catalog A had been statted, then restored A before validation and final checks. The pinned pool consequently certified and read B while pathname checks certified A. The focused repair must acquire A once with no-follow `os.open`, bind SQLite to that exact guarded file description without another lookup of the caller pathname, verify and close both handles on every path, and reproduce the connect-boundary substitution. |
| 2026-08-23 | M4-B / [PR #26](https://github.com/JapmeetBrar/pufferlab/pull/26) | `implementing → implementing` (exact-handle review-repair handoff) | The read-only catalog now uses POSIX `O_RDONLY|O_NOFOLLOW|O_CLOEXEC` acquisition, binds SQLite only to the verified guarded `/dev/fd/<fd>` description with immutable read-only URI flags, and fails closed when exact descriptor binding is unavailable. Guard identity is checked before, between, and after database validation and again around SQLAlchemy disposal; the SQLite connection and guard each close exactly once before the original path/sidecars receive their final check. A deterministic `sqlite3.connect` hook moves valid B into A's original pathname only during the real connect, proves the hook executed, and still reads A before restoring exact byte/inode/size/mtime/sibling snapshots. Thirty read-only tests pass three consecutive runs; the combined doctor/read-only 57-test suite, strict mypy, Ruff/format, full `make check` (650 backend tests plus one opt-in skip; 49 frontend tests/build), generated drift, artifact audit `238/674/19`, synthetic secret scan `238/674/25`, and diff checks pass. No real key, provider/network request, migration/write, sidecar, or user server was used. Root must inspect this repair and request another exact-head re-review. |
| 2026-08-23 | M4-B / [PR #26](https://github.com/JapmeetBrar/pufferlab/pull/26) | `implementing → review_requested` (exact-handle repaired whole head) | Root inspected the descriptor-bound acquisition and exact-close tests, then independently reran all 57 doctor/read-only tests and strict typing. Full `make check` passes with Ruff, formatting, strict mypy, 650 backend tests plus one opt-in live skip, OpenAPI drift, frontend lint/typecheck, 49 tests, and production build. The full-history artifact audit passes `238/677/19`; the exact configured-key scan passes across 238 tracked files, 677 Git blobs / 13,258,378 bytes, and 25 production browser files. Generated and diff checks are clean. No provider request, namespace operation, credential output, user-server access, or port 8000/5173 use occurred. The immutable repaired head now requires dedicated exact-head re-review; only that reviewer may merge. |
| 2026-08-23 | M4-B / [PR #26](https://github.com/JapmeetBrar/pufferlab/pull/26) | `review_requested → changes_requested → implementing` (third re-review) | [Third dedicated re-review](https://github.com/JapmeetBrar/pufferlab/pull/26#issuecomment-5387885788) found that Ubuntu canonicalizes the guarded `/dev/fd` SQLite name to `/proc/self/fd`, whole-leaf resolution can follow a transient substituted symlink before no-follow acquisition, and opening a FIFO read-only can block indefinitely. The doctor worker is assigned the smallest auditable repair: accept only an identity-verified same-descriptor alias, acquire the leaf parent-relatively without following it, reject nonregular files through nonblocking acquisition, and add exact race/resource regressions. |
| 2026-08-23 | M4-B / [PR #26](https://github.com/JapmeetBrar/pufferlab/pull/26) | `implementing → implementing` (third read-only repair handoff) | The catalog now resolves only the caller's parent, guards that exact directory, and acquires the leaf parent-relatively with `O_NOFOLLOW|O_NONBLOCK`; regular-file validation precedes clearing `O_NONBLOCK`, so writerless FIFOs and other nonregular nodes fail promptly without removal. SQLite receives only the guarded `/dev/fd/<fd>` URI, while post-connect validation accepts only the same numeric `/dev/fd` or Linux `/proc/self/fd` alias after an independent open/fstat identity match. Transient resolve/open leaf-symlink attacks cannot select replacement B, the existing connect-boundary A/B attack still reads A, and normal/error/cancellation/SystemExit plus FIFO paths close both parent and leaf descriptors exactly once. Forty focused tests pass three consecutive runs; the combined 67-test doctor/read-only suite, Ruff/format, strict mypy, full `make check` (660 backend tests plus one opt-in skip; 49 frontend tests/build), OpenAPI/generated-TypeScript drift, post-commit artifact audit `238/681/19`, synthetic secret-boundary scan `238/681/25`, and diff checks pass. No real key, provider/network request, migration/write, sidecar, user server, or port 8000/5173 was used. Actual Linux alias behavior remains for GitHub CI when root pushes the inspected whole head. |
| 2026-08-23 | M4-B / [PR #26](https://github.com/JapmeetBrar/pufferlab/pull/26) | `implementing → review_requested` (third repaired whole head) | Root inspected the guarded parent/leaf acquisition, finite alias allowlist, nonblocking reset, and adversarial resource tests, then independently reran all 67 doctor/read-only tests and strict typing. Full `make check` passes with Ruff, formatting, strict mypy, 660 backend tests plus one opt-in live skip, OpenAPI drift, frontend lint/typecheck, 49 tests, and production build. The full-history artifact audit passes `238/681/19`; the exact configured-key scan passes across 238 tracked files, 681 Git blobs / 13,556,781 bytes, and 25 production browser files. Generated and diff checks are clean. No provider request, namespace operation, credential output, user-server access, or port 8000/5173 use occurred. The immutable repaired head requires exact-head Ubuntu CI plus dedicated re-review; only that reviewer may merge. |
| 2026-08-23 | M4-B / [PR #26](https://github.com/JapmeetBrar/pufferlab/pull/26) | `review_requested → changes_requested → implementing` (fourth re-review) | [Fourth dedicated re-review](https://github.com/JapmeetBrar/pufferlab/pull/26#issuecomment-5387948410) confirmed that Ubuntu's SQLite VFS can canonicalize the guarded descriptor to the original disk pathname, so neither `/dev/fd` nor `/proc/self/fd` is a portable exact-binding proof. The doctor worker is assigned a bounded `pread` snapshot from the already guarded leaf, deserialization into one in-memory SQLite handle, prompt oversize/short/growth/identity rejection, complete buffer/error scrubbing, and portable adversarial lifecycle tests. |
| 2026-08-23 | M4-B / [PR #26](https://github.com/JapmeetBrar/pufferlab/pull/26) | `implementing → implementing` (portable snapshot repair handoff) | The read-only catalog no longer gives SQLite any disk, caller, resolved, descriptor, or procfs pathname. It verifies the guarded parent/leaf, reads an exact image with fixed 1 MiB `pread` chunks, rejects zero, short, growth, identity drift, and files above 256 MiB before SQLite, deserializes only into one `:memory:` connection, and wipes/clears the temporary bytearray on success and every failure/control path. The 256 MiB bound deliberately allows substantial headroom for canonical metadata, 50 queries/qrels, and 200 outcomes while bounding both the temporary image and SQLite's separate memory copy; larger accumulated catalogs fail closed and must be compacted or scoped separately. Deterministic connect-time A/B substitution still reads A; swap/truncate/grow/short snapshot attacks fail closed and restore exact file snapshots; no disk SQLite filename appears; and memory SQLite plus parent/leaf descriptors close exactly under errors, cancellation, and `SystemExit`. Fifty-four focused tests pass three consecutive runs; the combined 81-test doctor/read-only suite, Ruff/format, strict mypy, full `make check` (674 backend tests plus one opt-in skip; 49 frontend tests/build), OpenAPI/generated-TypeScript drift, post-commit artifact audit `238/685/19`, synthetic secret-boundary scan `238/685/25`, and diff checks pass. No real key, provider/network request, migration/write, sidecar, user server, or port 8000/5173 was used. Root inspection and another exact-head re-review remain required. |
| 2026-08-23 | M4-B / [PR #26](https://github.com/JapmeetBrar/pufferlab/pull/26) | `implementing → review_requested` (portable repaired whole head) | Root inspected the bounded `pread` snapshot, in-memory-only SQLite composition, buffer erasure, and mutation/cleanup attacks, then independently reran all 81 doctor/read-only tests and strict typing. Full `make check` passes with Ruff, formatting, strict mypy, 674 backend tests plus one opt-in live skip, OpenAPI drift, frontend lint/typecheck, 49 tests, and production build. The full-history artifact audit passes `238/685/19`; the exact configured-key scan passes across 238 tracked files, 685 Git blobs / 13,868,324 bytes, and 25 production browser files. Generated and diff checks are clean. No provider request, namespace operation, credential output, user-server access, or port 8000/5173 use occurred. The immutable portable head requires exact-head Ubuntu CI plus dedicated re-review; only that reviewer may merge. |
| 2026-08-23 | M4-D / `codex/m4-gate-core` | `planned → assigned → implementing` | The gate-core worker started from verified `main` `82e2f59`. Ownership is limited to the pure deterministic policy evaluator, its exhaustive hand-calculated and property tests, and this ledger. Acceptance requires exact aggregate paired deltas, exact per-query drop checks, canonical-50 error rate, coverage, equality-pass boundaries, finite-only inputs and safe finite reports, complete outcome/query identity validation, bounded deterministic violations, no query text, and no API/CLI/SQLAlchemy/provider/frontend/persistence/network/filesystem behavior imports. |
| 2026-08-23 | M4-D / [PR #27](https://github.com/JapmeetBrar/pufferlab/pull/27) | `implementing → review_requested` | Commit `6bd7d8f` adds the pure `evaluate_gate` engine and 37 exhaustive gate/import/privacy tests. Three consecutive focused runs pass; the broader evaluation/M4-contract suite passes 102 tests. `make check` passes Ruff, formatting, strict mypy over 109 source files, 710 backend tests plus one opt-in live skip, OpenAPI drift, frontend lint/typecheck, 49 tests, and production build. Generated TypeScript has zero diff; artifact audit passes `240/686/19`; the synthetic exact privacy scan passes across 240 tracked files, 686 history blobs / 13,997,208 bytes, and 25 production browser files; import-boundary and diff checks pass. Hand calculations prove four-way equality, paired-population means, hidden large regression detection, exact 0–49 candidate failure rates, coverage/exclusion semantics, all three metrics, global/bounded violation ordering, zero-pair invalidity, and hostile identity/non-finite failures. No provider/network/model/database/filesystem/CLI/API/frontend behavior or real credential was used. M4-E still owns authenticated durable recomputation, read-only composition, rendering, and process exits; dedicated exact-head review and reviewer-only merge are next. |

## Milestone 1 acceptance evidence

- [x] One documented command ingests the tiny fixture into a uniquely owned turbopuffer namespace.
- [x] Re-running ingestion is idempotent and readiness is verified.
- [x] The browser submits one query through FastAPI and receives BM25 and vector result lists.
- [x] Results show document identity, 1-based rank, typed score semantics, and client wall-clock time.
- [x] The API key remains server-side and is absent from repository history and browser assets.
- [x] Fake-provider tests run in normal CI; the opt-in live test creates and cleans its namespace.
- [x] Backend, frontend, and generated-contract checks pass locally; PR checks are green at the last
  reviewed implementation head. Finalization-head and default-branch checks remain canonical in
  GitHub.
- [x] Every preceding delivery PR records an independent reviewer decision and merge. Finalization
  PR #9's own verdict, merge, and post-merge checks remain canonical in GitHub.

## Milestone 2 acceptance evidence

- [x] Pure judged metrics match hand calculations and a trusted reference across edge cases.
- [x] Immutable revisions, incremental outcomes, interruption recovery, and cancellation persist in
  SQLite under the ignored data directory.
- [x] The deterministic Unix pack preserves attribution/qrels and yields a curated 50-query suite
  without tracking raw licensed data.
- [x] BM25, ANN, server RRF, and local-reranked configurations return contract-valid evidence and
  separated client timings.
- [x] One CLI command completes and persists the 50-query/four-configuration run; JSON export
  validates against the checked-in contract.
- [x] The credentialed run cleans only its exact generated namespace and retains no secret, raw
  vector, corpus, or database artifact in Git.
- [x] Every Milestone 2 PR receives independent review, reviewer-only merge, and green protected
  `main` checks.

### Sanitized live run

- Region: `gcp-us-west1`; namespace SHA-256 fingerprint: `505a2788e77e`; cleanup:
  `not_found_verified` with no retained session record.
- Pinned archive: 5,343,728,040 bytes, MD5 `4e41456d7df8ee7760a7f866133bda78`, SHA-256
  `6072f7d345496387d24194c8af35c5fb6c2f0e5f9130c5b4a78b98bbfac88558`; processed-pack SHA-256
  `6d54fb92c04b9f193d081a7c430d8804e24e71855d3cbaa2bb50cde838f181b8`.
- Dataset/query/run: `dd403bda-d298-5a4a-bb45-a29dee84c61b` /
  `e490c239-69ee-57d6-b6b0-bb5cc31c8b2a` / `34af9297-e423-4787-a43b-37292aaf1d93`;
  47,382 documents, 50 queries, 83 curated qrels, 200 successful outcomes.
- Canonical export SHA-256:
  `46b175d9cd88e2f102876c00c5e3a60e533c8dd0cea07118b215efef0ba83e02`.

| Mode / config | NDCG@10 | Recall@50 | MRR@10 | p50 ms | p95 ms | Error rate |
|---|---:|---:|---:|---:|---:|---:|
| BM25 / `66dcf586-d65e-5a36-9875-5bcb76a5630e` | 0.330995 | 0.488000 | 0.363190 | 201.502 | 275.288 | 0.000000 |
| ANN / `93f50bf2-6327-5564-8823-ceba259d5d70` | 0.456537 | 0.696667 | 0.511500 | 504.903 | 959.744 | 0.000000 |
| Server RRF / `59385c3a-bfa8-548f-996b-11ca01bbdb94` | 0.418917 | 0.675000 | 0.455627 | 546.025 | 1,198.534 | 0.000000 |
| Local reranker / `db981631-4a65-597a-96f7-70ab850a4e24` | 0.435933 | 0.675000 | 0.498667 | 1,451.313 | 2,460.539 | 0.000000 |

Every displayed aggregate has sample count 50; the verifier independently recomputed quality from
ranked IDs plus exact qrels and latency percentiles from stored per-query client wall times.

## Milestone 3 acceptance evidence

- [x] Versioned, generated contracts expose persisted catalogs, run list/detail, cancellation,
  regressions, query detail, export, and explicitly labeled live replay.
- [x] Provider-free dashboard reads work for completed, partial, cancelled, interrupted, and failed
  runs without mutating durable state or triggering network calls.
- [x] A canonical run can be started at HTTP 202, polled across refresh, cancelled idempotently, and
  remains inspectable with all completed outcomes preserved.
- [x] Regression ordering uses candidate-minus-baseline quality deltas, shows sample/coverage state,
  and deep-links into exact run/query/config context.
- [x] Missing original stage evidence is `NOT_OBSERVABLE`; an optional live replay is exact-bound,
  cost-bearing, and visibly distinct from recorded evidence; primary and counterfactual-probe
  observations are never conflated.
- [x] A clean checkout can idempotently seed and serve an explicitly synthetic 50-query/four-config
  completed run from SQLite without provider access, credentials, licensed text, or tracked database
  and export artifacts; it is read/export-only, has recomputed quality, and reports timing as
  unavailable rather than observed.
- [x] The dashboard and forensic drawer pass generated-type, accessibility, responsive, browser,
  secret/artifact, and production-build gates.
- [x] Every Milestone 3 PR receives independent review, reviewer-only merge, and green protected
  `main` checks.

All Milestone 3 product and process acceptance items are satisfied. PR #23's independent verdict,
reviewer-only merge, and protected-main checks are canonical in GitHub.

## Milestone 4 acceptance evidence

- [ ] M4 architecture, local-versus-live capability semantics, cleanup authority, gate policy,
  branch graph, rollback, and non-goals are independently reviewed before implementation.
- [ ] Doctor/serve and the Playground expose actionable local configuration state without provider
  calls, secret values, namespace values, model initialization, or remote-health claims.
- [ ] Generated tiny ingestion durably owns and resumes one exact HMAC-derived namespace; cleanup
  accepts no caller target/path/token and retains its authenticated receipt on every failure.
- [ ] A completed durable run can produce a provider-free CI gate verdict with source-authenticated
  Unix/synthetic judgments, structurally validated outcomes, recomputed quality over trusted local
  ranked IDs, bounded safe output, and stable pass/policy/invalid/internal exit codes.
- [ ] Required Playwright/axe CI reproduces the provider-free synthetic desktop/mobile/history/focus
  journey and proves action-required Playground state sends no comparison POST.
- [ ] Every Milestone 4 PR receives independent review, reviewer-only merge, and green protected
  `main` checks after the single goal-finalization PR.

## Decision log

| Date | Decision | Reason |
|---|---|---|
| 2026-08-22 | Use one focused `codex/*` branch and GitHub PR per review unit. | Keeps diffs small, independently reversible, and easy to review. |
| 2026-08-22 | Keep a dedicated reviewer separate from implementation workers. | Prevents self-approval and creates a consistent quality gate. |
| 2026-08-22 | Track execution in this repository instead of chat-only state. | Survives agent handoffs and makes blockers and evidence auditable. |
| 2026-08-22 | Use fake provider tests in CI and opt-in isolated live tests. | Keeps CI deterministic while validating the real account path safely. |
| 2026-08-22 | Treat provider documentation as a contract-review input. | Independent review caught dense-vector and FTS constraints before provider implementation. |
| 2026-08-22 | Make GitHub merge/check history canonical between finite ledger updates. | Avoids recursively opening a PR solely to record the previous ledger PR's merge. |
| 2026-08-22 | Protect `main` with active ruleset `21190317` requiring PRs and Backend/Frontend checks. | Enforces review-unit and CI boundaries without bypasses; GitHub approvals remain zero because agents share one identity. |
| 2026-08-22 | Split Milestone 2 into four parallel foundations, one serial integration PR, and one finalization PR. | Keeps pure metrics, persistence, retrieval, and licensed-data handling independently reviewable while enforcing a single integration point. |
| 2026-08-22 | Keep downloaded CQADupStack data, generated embeddings, SQLite, and live evidence outside Git. | Preserves licensing, credential, reproducibility, and repository-size boundaries while checked-in manifests and hashes make processing auditable. |
| 2026-08-23 | Make durable SQLite read models the dashboard source and live replay a separate explicit observation. | Preserves offline/partial-run inspection and prevents a new query from being misrepresented as original run evidence. |
| 2026-08-23 | Keep Milestone 3 P0 canonical at 50 queries and four immutable configs. | Delivers the interview workflow without reopening general config editing, arbitrary suites, distributed execution, or multi-tenant scope. |
| 2026-08-23 | Reclaim valid durable queued work at one-worker startup, interrupt stale running work, and fail invalid queued bindings safely. | Closes the persist-before-schedule crash gap without pretending partial provider work can be resumed safely. |
| 2026-08-23 | Label primary live replay and counterfactual provenance probes per observation. | Separate provider requests can observe different snapshots and must never be presented as one causal trace. |
| 2026-08-23 | Seed the offline demo through M3-B into SQLite from deterministic synthetic inputs. | Gives a fresh checkout an honest full dashboard path before finalization without tracking licensed data, provider evidence, databases, or exports. |
| 2026-08-23 | Make `synthetic_demo` read/export-only with unavailable timing and normally recomputed quality. | Prevents demo identifiers from authorizing provider cost and avoids fabricating latency for searches that never ran. |
| 2026-08-23 | Scope Milestone 4 to operator readiness, authenticated generated-tiny cleanup, provider-free CI gates, and repeatable browser/accessibility CI. | Closes the reproduced setup and original hardening gaps while adding one bounded P1 deployment feature; generic customer import and namespace branching require separate schema, licensing, and ownership work. |
